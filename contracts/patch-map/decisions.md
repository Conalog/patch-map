# PatchMap decisions

This registry owns closed product decisions and their evidence obligations.
`Resolved` means implementations follow the decision; execution and release
approval remain separate evidence states.

Each accepted input belongs to the current closed schema. Removed fields and aliases
are rejected rather than normalized through a compatibility path.

| ID | Status | Decision | Affected contract | Normative rule |
| --- | --- | --- | --- | --- |
| OQ-002 | Resolved | Production performance workload | PRF-001–007 | Measure both the exact 100/500/1,000/2,000/5,000 synthetic matrix and a representative production-shaped fixture with materialized element/component/text/relation counts. Production approval waits for the fixture digest and raw samples. |
| OQ-003 | Resolved | Group/ungroup product scope | CSM-031, HIS-006 | Group and ungroup are first-class PatchMap editor actions. They atomically preserve hierarchy, selection, relations, host companion state, and one history action. |
| OQ-004 | Resolved | Interactive resize eligibility | TRN-003/004, CSM-023 | Only standalone `rect` and `image` elements expose transformer resize handles. Other kinds may move/rotate where declared. Dataset transactions may still change schema-supported size fields atomically; this decision does not narrow ordinary programmatic updates. |
| OQ-005 | Resolved | ID scope | DAT-005/008, UPD-001, QRY-001 | Element IDs are scene-global; component IDs are unique within their owning item; generated grid-cell IDs are deterministic composites. Addressing uses scope plus owner where required and never selects an arbitrary duplicate. |
| OQ-006 | Resolved | Non-JSON PixiJS colors | DAT-004, DET-001/004 | Accept PixiJS v8 public `ColorSource` values at load/update. Normalize semantic intent to 8-bit RGBA and canonical export to lowercase `#rrggbbaa`; caller objects remain unchanged. Invalid/non-finite color input fails atomically. |
| OQ-009 | Resolved | Hidden grid relation endpoints | DAT-005, REN-007, UPD-010 | A hidden cell retains deterministic endpoint identity and geometry, but every relation segment using it is hidden. Showing the cell restores the same relation without recreating unrelated identity/history. |
| OQ-010 | Resolved | Shared asset lifetime | AST-001/003, PRF-007 | Use a global PixiJS asset/cache entry with per-Core-instance leases. Instance destroy releases its leases; the underlying resource unloads only after the last lease and pending user are gone. |
| OQ-011 | Resolved | Initialization asset failure/retry | LIF-001, AST-001/003, ERR-003/006 | Only assets explicitly declared required for initialization block readiness. Their failure rejects init and returns cleanly to `new`; destroy/retry cancels and releases late work. Scene assets use OQ-034. |
| OQ-012 | Resolved | History ownership and capacity | HIS-002/004, CSM-034 | PatchMap owns a configurable history stack with default capacity 50, host-callable undo/redo/clear and availability, plus standard Ctrl/Cmd shortcuts outside protected editable surfaces. |
| OQ-013 | Resolved | Third and later clicks | EVT-002, SEL-002 | Count 1 emits only `single`, count 2 only `double`, and count 3+ exactly one generic `multi-click` callback containing the actual click count. One physical action never emits multiple semantic click callbacks. |
| OQ-014 | Resolved | Touch tap/click duplication | EVT-001/002 | Deduplicate native tap/click/pointer aliases by physical pointer-gesture identity and deliver exactly one semantic click-count outcome. |
| OQ-015 | Resolved | Overlapping direct/query bindings | EVT-005 | Direct and query bindings resolving to the same logical target/action deliver one callback total. Binding multiplicity never creates duplicate semantic delivery for that target/action. |
| OQ-016 | Resolved | Event propagation | EVT-005/006 | Use capture → target → bubble ordering with deterministic stop/immediate-stop behavior. Empty-surface events use the same phases with a null logical target. |
| OQ-017 | Resolved | Selection without overlay | SEL-001–006, TRN-001 | Semantic selection is independent of transformer/outline presentation; overlays are optional. |
| OQ-018 | Resolved | Selection/lock change during transform | TRN-003/009 | Cancel the active transform, restore its start state, emit no transform history, and require a new gesture. |
| OQ-019 | Resolved | Selection after redraw | LIF-003, SEL-008 | Rebind only explicit host-supplied selection IDs that still exist; otherwise clear engine-local selection. |
| OQ-020 | Resolved | Editable shortcut suppression | HIS-004, EVT-006 | Do not intercept history shortcuts from input, textarea, select, contenteditable, or an editable target in an open shadow-root composed path. An iframe document owns and handles its own keyboard events. |
| OQ-021 | Resolved | Hidden/unmaterialized component identity | REN-003/008–011, UPD-008 | Keep the logical component and query identity while omitting its PixiJS render object. Showing it materializes presentation from current semantic state without creating a new logical ID. |
| OQ-022 | Resolved | Dimension and nonnegative spacing rules | DAT-003, REN-004/009 | In the component dimension grammar, reject plain numeric strings and `px` scalar strings. Accept documented percentage/`calc()` dimensions and text-style CSS size branches. Reject negative margin, padding, animation duration, and border width. |
| OQ-023 | Resolved | Self/duplicate relations | REN-007, UPD-010 | Allow finite self-links. Deduplicate identical ordered `(source,target)` pairs deterministically while preserving first authored order; reverse direction remains distinct. |
| OQ-024 | Resolved | Supported runtime/backend | PIX-003/005, PKG-001/004/005, PRF-001–009 | Support Windows 10/11 and the latest two stable Chrome and Edge versions at release time. WebGL2 is mandatory; WebGPU is optional and cannot weaken semantics. Each release pins exact browser, PixiJS v8, DPR, CSP, TypeScript, bundler, and package evidence. |
| OQ-025 | Resolved | Numeric performance budgets | PRF-001–009 | On the approved low-end Windows profile: frame-gap p95 ≤33 ms, action-to-visible p95 ≤50 ms, and zero main-thread tasks ≥100 ms during required scenarios. Preserve raw samples and workload-specific phase/memory budgets. |
| OQ-026 | Resolved | Asset/security ownership | AST-001–003, SEC-001–003, OPS-001/002, PIX-004 | Host supplies explicit protocol/origin/CORS/credential/media/size/decode allowlists; PatchMap enforces them before fetch/decode/upload. SVG scripts/external resources are blocked, secrets/data URIs are redacted, and failures preserve the live scene. |
| OQ-028 | Resolved | Accessibility ownership | ACC-001–003, EVT-006, SEL-004, HIS-004 | PatchMap owns a logical accessibility tree and keyboard action API and connects visible interactive objects to PixiJS AccessibilitySystem. Host DOM overlays remain host-owned. Accessibility activation deduplicates PixiJS click/tap aliases into one semantic action. |
| OQ-029 | Resolved | Input device matrix | EVT-001–008, VIE-001/007/008, ACC-001/002, LIF-006 | Mouse, precision trackpad, and keyboard are mandatory, including supported browser zoom, host CSS transform, scrolling, and DPR-change cells. Touch, pen, and multi-pointer are supported but each release cell is approved only with real capable Windows-device evidence; simulation alone cannot approve it. |
| OQ-030 | Resolved | `angle` plus `rotation` | DAT-001/003/007, LAY-004/005, VIE-005, DET-004 | Reject a record/transaction containing both fields atomically. Valid single-field input preserves degrees or radians respectively. |
| OQ-031 | Resolved | International text | REN-006/011, PRF-004, DET-002 | Grapheme segmentation, CJK wrapping, bidi/RTL, emoji sequences, combining marks, fallback fonts, and missing glyph outcomes are normative semantic layout and require the declared corpus. Raster pixels remain environment-qualified. |
| OQ-032 | Resolved | Gesture interruption | EVT-004, TRN-009, ERR-004 | Normal pointer-up, including outside, commits once. Escape, explicit/pointer cancel, lost capture, blur, and redraw revert to the start state, release ownership, and create no gesture history. Selection/lock changes follow OQ-018; replacement/destroy terminate the old lifecycle without stale completion. |
| OQ-033 | Resolved | Bar animation | ANI-001–003, REN-009, PRF-003 | Use `easeOutCubic` over the dataset duration, default 200 ms. Commit the destination immediately, interpolate presentation from the current visible value, clamp within the declared track, publish the exact destination at `t >= duration`, and emit one settled outcome. |
| OQ-034 | Resolved | Blocking versus optional assets | LIF-001/002, AST-001–003, ERR-003, REN-005/008/010 | Only explicitly required initialization assets block readiness. Scene assets fail target-locally with deterministic placeholder geometry/hit behavior and retry; unrelated scene content and first useful frame remain usable. |
| OQ-035 | Resolved | Dataset versioning | dataset schema, DET-004, PKG-004/005 | Do not add a dataset version field, envelope, or negotiation requirement now. Preserve the existing array root and schema. Introduce a version plus migration contract before, and only when, a future schema change is approved. |
| OQ-036 | Resolved | Relation hit tolerance | REN-007, SEL-001/005/006, CSM-013/020/026 | Point/box/paint hit expansion is `max(4 CSS px, visible stroke width / 2)` in screen space after zoom/DPR; semantic path geometry itself is unchanged. |

All product decisions are closed and all 32 canonical fixture/expected pairs are
analysis-owner contract-approved. The 169-record catalog binds the broader capability
and consumer-journey contract. Automation, Lab implementation, packed-host integration,
target-Windows runs and accessibility/device evidence
remain execution work; they are not missing product decisions or contract evidence.
