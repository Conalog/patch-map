# Goal

Connect all 38 approved decisions and 173 cases to PixiJS product behavior, independent actual automation, the same focused Lab route, packed integration, and production-readiness evidence without changing approved expected observations.

# Scope

- Implement approved records in 5–10 case dependency tranches with shared product and expected-blind automation substrate.
- Work only on `performance/core-v2`; keep immutable fixtures/expected/review evidence, Core v1, clean-room/comparison paths, dependency internals, and other refs/worktrees frozen.
- Preserve PATCH MAP input immutability, stable identity, atomic failure, aggregate Pixi WebGL rendering, and explicit lifecycle/resource ownership.

# Current Facts

- Contract revision `core-v2-functional-contract/2026-07-16.2` contains 38 approved decisions, 135 capabilities, 38 journeys, 646 actions, and 1,388 assertions. Canonical verification and all 32 negative drift probes pass unchanged.
- Core v2 uses a dense store, aggregate Mesh/asset/text/interaction layers, root-only event authority, one manual scheduler, and explicit scene-versus-frame publication. WebGL2 is mandatory; WebGPU and native Windows remain pending.
- `DAT-008` truthfully stops at its malformed approved action. Twenty immutable conflicts are observed and two `UPD-007` conflicts remain declared latent; none are hidden or aliased.
- Default PATCH MAP loading remains compatibility-permissive for dangling relations, while an explicit strict load option performs exact duplicate/reference validation before publication and preserves the prior scene atomically on failure.

# Current State

- Coverage is 135 executable routes, 134 actual-producing routes, 38 explicit stubs, 514 executable actions, and 286 action types. `DAT-008` remains the sole executable route that truthfully stops before actual production on its malformed approved action.
- `ERR-003`, `AST-002`, `AST-003`, `SEC-001`, and `CSM-032` are closed in product `ae5097b` and shared expected-blind automation/Lab `2eb941b`. One asset-ingestion runtime covers target retry, shared binding generations, descriptor identity, stale A/B completion, prefetch security policy, host clipboard/drop planning, atomic rollback, deterministic observations, and cleanup.
- The full unit run produced 1,437 passing tests and exposed one stale static route-count assertion. After updating that inventory pin, its targeted file passes 11/11; full lint, typecheck, Core v2/Lab builds, and canonical 38-decision/32-negative/173-record verification pass.
- The five changed headless WebGL routes pass first/repeat/fresh/destroy at 52 matches plus the one declared `AST-002` conflict out of 53 assertions per session, with deterministic digests, one transient canvas returning to zero, and zero console/page/network/external-fixture errors.
- The browser registry now covers 120 routes and 1,641 assertions: 1,620 expected matches, twenty-one observed immutable conflicts, and two latent conflicts. `AST-002` truthfully reports the allowed `INVALID_VALUE` code against immutable expected `INVALID_ASSET_DESCRIPTOR`; neither product diagnostics nor expected evidence were altered to hide it.
- Packed ESM/CJS consumption passes after the Core v2 subpath export change. The 2+7 lifecycle gate covers 5,099 entities with retained-heap median 78,611 bytes and releases DOM, scheduler, renderer, asset, and pending-work ownership.
- The unchanged full browser matrix and performance matrix were not repeated because only the five new routes and renderer-facing asset lifecycle changed; the targeted routes and required lifecycle gate cover that risk. Headed final release verification, WebGPU, native Windows, and final independent release review remain pending.

# Next Step

Work is intentionally paused at a clean asset-ingestion checkpoint. After restart:

1. Re-run the mandatory policy/BRIEF read and verify the recorded branch/status before changing files.
2. Inventory the remaining 38 explicit stubs from the canonical descriptors and choose the next shared-substrate tranche of 5–10 related cases.
3. Record that tranche's exact case/action/probe boundary before implementation; do not reopen the closed asset-ingestion tranche unless a regression demonstrates a product defect.
4. Continue with targeted tests first and apply the approved risk-based full/expensive gate cadence at tranche promotion.

# Working Boundary

- `src/core-v2/`
- `tests/core-v2/`
- `lab/performance-v2/`
- `scripts/verification/`
- `docs/tasks/2026/07-15/performance-core-v2/`
