# Goal

Connect all 38 approved decisions and 173 cases to PixiJS product behavior, independent actual automation, the same focused Lab route, packed integration, and production-readiness evidence without changing approved expected observations.

# Scope

- Implement all approved records without regenerating, weakening, or editing canonical fixtures, normalized expected evidence, review records, or decision outcomes.
- Work only on `performance/core-v2`; keep Core v1, clean-room/comparison paths, prior evidence, dependency internals, and other refs/worktrees frozen.
- Advance in 5–10 case dependency tranches with shared product/automation substrate and risk-based gates. Prefer headless browser automation; reserve headed runs for behavior that headless cannot establish and final explicit checkpoints.

# Current Facts

- Contract revision `core-v2-functional-contract/2026-07-16.2` contains 173 records, 646 actions, and 1,388 assertions; all journeys are migration-blocking P0.
- Promotion requires expected-blind product execution, independent comparison, the exact focused route, declared packed-host evidence, cleanup, determinism, and review.
- Core v2 accepts PATCH MAP arrays directly, preserves immutable input and stable identity in a dense store, and renders aggregate Mesh/WebGL layers rather than per-entity Pixi objects.
- WebGL2 is mandatory. Chromium 4× is a proxy; WebGPU and native Windows remain pending.

# Current State

- Canonical verification passes 38 decisions, 32 negative probes, and all 173 records unchanged. Forty-three routes are executable, 42 produce actuals, `DAT-008` truthfully fails at its missing `as`, and 130 remain explicit stubs. The closed diagnostic registry and every documented immutable-expected conflict remain unchanged.
- The transaction authority now also closes structural case `UPD-009`: cross-parent move, group, ungroup, order and world-transform preservation, logical group/component selection, relation refresh, one history unit per recorded action, unrecorded mutation, undo/redo companions, and atomic cycle/locked-hierarchy refusal all reconcile into aggregate dense layers without per-entity Pixi ownership.
- One shared expected-blind handler, fold, runtime, and focused-Lab shell serve all nine synchronous update routes; product/Lab code imports no normalized expected or comparison data. Headless Chromium 143 passes 28 routes first/repeat/fresh at 387/393 per session, preserving six observed immutable conflicts plus two declared latent conflicts, with real WebGL2 draws, canvas cleanup zero, and zero console/page/network/external-fixture errors.
- Milestone gates pass 141 files/1,158 unit tests, full lint/typecheck, Core v2/Lab builds, canonical verification, and packed ESM/CJS hierarchy consumption. The packed run proves input immutability, selection/history, atomic cycle rejection, relation revision correlation, capture, and destroy cleanup. Memory and performance matrices were not repeated because structural work changed neither renderer/resource ownership nor a measured hot path; the prior 2+7 and six-scale evidence remains the current unaffected checkpoint.
- The final Chromium 4x 2+7 matrix preserves 162 measured raw trials across all six scales. Mesh remains the selected production renderer because large-scene full/partial updates dominate Particle, while Particle remains faster in several small-scene and production first-frame/prepare intervals. WebGPU is unavailable and native Windows remains pending.

# Next Step

Implement the four-case async/presentation tranche `UPD-011/012/013/014`. Keep it smaller than the normal 5–10 case tranche because it changes asynchronous ownership, destroy cancellation, ephemeral presentation policy, high-frequency coalescing, and renderer invalidation together. Build one latest-revision work registry, one persisted-data-independent presentation overlay, accepted-versus-visible revision publication, and targeted semantic refresh; expose these through one expected-blind descriptor/runtime and the same focused Lab shell. Use targeted tests while building and one tranche full unit/lint/typecheck/build/verifier checkpoint. Re-run headless WebGL and 2+7 memory because publication/resource ownership changes; run the update performance checkpoint because the 60 Hz overlay hot path changes; run packed consumer only if the public package/host seam changes.

# Working Boundary

- `src/core-v2/`
- `tests/core-v2/`
- `lab/performance-v2/`
- `scripts/verification/`
- `docs/tasks/2026/07-15/performance-core-v2/`
