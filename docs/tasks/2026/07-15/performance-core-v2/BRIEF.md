# Goal

Connect every approved Core v2 functional-contract decision and case to the PixiJS Core v2 implementation, independent actual-observation automation, the same focused Lab route, packed integration, and release evidence. Completion means 38/38 decisions and all 135 capabilities plus 38 consumer journeys satisfy the immutable expected observations and production-readiness gates.

# Scope

- Implement all 173 approved records without regenerating, weakening, or editing canonical fixtures, normalized expected evidence, review records, or decision outcomes.
- Work only on `performance/core-v2`; keep Core v1, clean-room/comparison paths, prior evidence, dependency internals, and other refs/worktrees frozen.
- Advance in dependency-ordered tranches, pairing each implementation change with independent automation and its unique light-theme Lab route; run aggregate package, browser, memory, performance, security, accessibility, integration, and migration gates only at meaningful milestones.

# Current Facts

- Contract revision `core-v2-functional-contract/2026-07-16.2` contains 173 digest-bound records, 381 action definitions, 646 ordered actions, and 1,388 typed assertions. The capability registry has 83 P0 and 52 P1 cases; all 38 consumer journeys are migration-blocking P0.
- Canonical generation proves specification integrity only. Promotion requires expected-blind product execution, immutable actuals, independent comparison, the exact focused route, packed-host evidence where declared, cleanup, determinism, and review.
- Core v2 accepts array-root PATCH MAP JSON directly, uses a dense store and aggregate Mesh/WebGL renderer, preserves caller input and stable identity, and reports unsupported projection instead of restoring per-entity Pixi objects.
- WebGL2 is mandatory. Chromium 4× remains a development proxy; WebGPU is optional and native Windows Chrome/Edge, accessibility-device, security, migration, and target-performance evidence remain pending until executed.

# Current State

- `verify:core-v2-contract` passes 38 decisions, canonical plus 32 negative probes, and 173 approved records. No canonical expected or review artifact has changed.
- Fourteen P0 routes are connected: `LIF-001/002/004/005`, `DAT-001..008`, and `CSM-001/003`. Thirteen produce actual observations; `DAT-008` truthfully fails at immutable `retainTarget` missing `as`. The other 159 routes remain explicit stubs. Source journeys are not packed-host proof.
- Exact results include `LIF-001` 11/11, `DAT-002` 13/13, `LIF-004` 12/12, and `DAT-007` 10/10. Known mismatches remain in `LIF-002`, `DAT-001`, `DAT-006`, `DAT-008`, and source journey host leaves; twelve out-of-registry expected diagnostic assertions remain immutable contract conflicts.
- `CoreV2.reconcile` now converts a parsed candidate into one safe dense transaction, preserves same-ID ref/slot/selection, refuses unsupported reorder/background changes atomically, and never substitutes a full load. Strict materialization now covers all fields actually used by approved array datasets and production-like input, including image opacity, standalone overflow, event mode, and corner-radius tuples.
- Projection inventory still records incomplete behavior for `scaleX`, item `zIndex`, upright content orientation, background authored size semantics, text split/style details, relation cap/join, and asset descriptor resolution metadata.

# Next Step

Connect the authoritative Engine mutation surface to incremental reconcile, then close the rendering/layout/assets P0 tranche in dependency order. Every newly executable case receives an expected-blind handler/fold and the same focused Lab route before milestone package/browser evidence. Keep projection degradation explicit; do not promote source-only journeys or contract-conflicted rows.

# Working Boundary

- `src/core-v2/`
- `tests/core-v2/`
- `lab/performance-v2/`
- `scripts/verification/`
- `docs/tasks/2026/07-15/performance-core-v2/`
