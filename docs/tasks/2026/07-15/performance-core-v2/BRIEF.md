# Goal

Connect all 38 approved decisions and 173 cases to PixiJS product behavior, independent actual automation, the same focused Lab route, packed integration, and production-readiness evidence without changing approved expected observations.

# Scope

- Implement all approved records without regenerating, weakening, or editing canonical fixtures, normalized expected evidence, review records, or decision outcomes.
- Work only on `performance/core-v2`; keep Core v1, clean-room/comparison paths, prior evidence, dependency internals, and other refs/worktrees frozen.
- Advance in 5–10 case dependency tranches with shared product/automation substrate and risk-based gates. Prefer headless browser automation; reserve headed runs for behavior that headless cannot establish and final explicit checkpoints.

# Current Facts

- Contract revision `core-v2-functional-contract/2026-07-16.2` contains 173 records, 646 actions, and 1,388 assertions; all journeys are migration-blocking P0.
- Promotion requires expected-blind product execution, independent comparison, the exact focused route, declared packed-host evidence, cleanup, determinism, and review.
- Core v2 accepts PATCH MAP arrays directly, preserves immutable input and stable identity in a dense store, and renders aggregate Mesh/WebGL layers rather than per-entity Pixi objects.
- WebGL2 is mandatory. Chromium 4× is a proxy; WebGPU and native Windows remain pending.

# Current State

- Canonical verification passes 38 decisions, 32 negative probes, and all 173 records unchanged. Fifty-four routes are executable, 53 produce actuals, `DAT-008` truthfully stops at its malformed approved action, and 119 remain explicit stubs.
- `VIE-001/002/003/004/008` and `CSM-009/010` share one Engine view authority for root input, focus/fit, settle/serialization, restore fallback, policy lifecycle, resize, and remount. A private cooperative first-load candidate preserves stable slots and atomic publication while one Pixi renderer remains owned by the Engine.
- Core v2 unit tests pass 116 files/982 tests; lint, typecheck, both builds, canonical verification, packed ESM/CJS consumption, and the 39-route headless WebGL first/repeat/fresh checkpoint pass. Each browser session records 526/532 matches plus the six approved immutable conflicts, canvas cleanup zero, and zero console/page/network/external-fixture errors.
- The refreshed 2+7 lifecycle gate covers 5,099 entities with retained-heap median 86,059 bytes and releases DOM, scheduler, renderer, assets, listeners, and pending work. The latest quick 4× checkpoint preserves 2 warmups plus 7 samples at 100/1,000 records: Particle retains small-scene wins while Mesh remains materially stronger at 1,000 records and animation. WebGPU and native Windows remain pending.

# Next Step

Implement the six-case query/selection substrate tranche `QRY-001/002` plus `SEL-001/002/003/004`. Reuse the dense ID/component/relation indexes for immutable query snapshots, stale-result refusal, transformed point selection, selection-unit drill-down, eligibility filtering, and replace/add/remove/toggle/clear semantics; connect one declarative expected-blind runtime and the same focused Lab shell. Use targeted tests while building, then one tranche full unit/lint/typecheck/build/verifier checkpoint. Run headless browser only when real selection input or the shared Lab route changes; reserve package, memory, and performance gates for their risk conditions.

# Working Boundary

- `src/core-v2/`
- `tests/core-v2/`
- `lab/performance-v2/`
- `scripts/verification/`
- `docs/tasks/2026/07-15/performance-core-v2/`
