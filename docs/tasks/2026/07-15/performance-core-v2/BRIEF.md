# Goal

Connect all 38 approved decisions and 173 cases to PixiJS product behavior, independent actual automation, the same focused Lab route, packed integration, and production-readiness evidence without changing approved expected observations.

# Scope

- Implement all approved records without regenerating, weakening, or editing canonical fixtures, normalized expected evidence, review records, or decision outcomes.
- Work only on `performance/core-v2`; keep Core v1, clean-room/comparison paths, prior evidence, dependency internals, and other refs/worktrees frozen.
- Advance in 5–10 case dependency tranches with shared product/automation substrate and risk-based gates. Prefer headless browser automation; reserve headed runs for behavior that headless cannot establish and final explicit checkpoints.

# Current Facts

- Contract revision `core-v2-functional-contract/2026-07-16.2` contains 173 records, 646 actions, and 1,388 assertions; all journeys are migration-blocking P0.
- Promotion requires expected-blind product execution, independent comparison, the exact focused route, declared packed-host evidence, cleanup, determinism, and review.
- Core v2 accepts PATCH MAP arrays directly, preserves immutable input and stable identity in a dense store, and renders aggregate Mesh/WebGL layers rather than per-entity Pixi objects.
- WebGL2 is mandatory. Chromium 4× is a proxy; WebGPU and native Windows remain pending.

# Current State

- Canonical verification passes 38 decisions, 32 negative probes, and all 173 records unchanged. Eighty-three routes are executable, 82 produce actuals, `DAT-008` truthfully stops at its malformed approved action, and 90 remain explicit stubs.
- `VIE-001/002/003/004/008` and `CSM-009/010` share one Engine view authority for root input, focus/fit, settle/serialization, restore fallback, policy lifecycle, resize, and remount. A private cooperative first-load candidate preserves stable slots and atomic publication while one Pixi renderer remains owned by the Engine.
- `QRY-001/002` and `SEL-001/002/003/004` share an immutable logical scene index, revision-bound query handles, ordered selection-set reduction, hierarchy/lock eligibility, transformed point selection, and unit/drill resolution. Component identity stays owner-qualified, unconstrained point hit reuses the Core spatial index, and constrained resolution never exposes Pixi objects.
- `EVT-001/002/003/004/008` and `SEL-005/006` share one root pointer authority for normalized click/drag ordering, count/deduplication, hover exit, interruption, view-change suppression, box/paint region selection, relation intersections, and eligibility filtering. Pixi owns five federated stage listeners plus native wheel/leave/context-menu boundaries at the root; actual listener count is eight and entity callbacks remain zero.
- `EVT-005/006/007/009` and `SEL-008` share one logical host-interaction authority for stable query bindings, component-owner-surface propagation, ordered specific/family observation, keyboard and transformer isolation, nested interaction-mode ownership, explicit canvas selection publication, and external selection redraw/remount behavior. It uses root authority and logical descriptors rather than DOM, Pixi, or per-entity callbacks.
- `SEL-007/009` and `TRN-002/003/010` share one selection/transformer authority for oriented single frames, axis-aligned multi unions, CSS-stable handles/strokes, transformable subset projection, current-ID relation endpoint resolution, and exclusive root gesture ownership. Pixi renders one aggregate overlay and retains no endpoint snapshots, entity listener, ticker, or DisplayObject authority.
- `HIS-001..006` share one bounded semantic history authority: detached full snapshots, accepted-action grouping, branch/capacity control, keyboard and host controls, selection/mode/host companion state, ordered restore/publication events, and replace/clear/destroy cleanup. Structural create/duplicate/move/group/ungroup/delete and component order restore through exact command-boundary order permissions rather than a fake metadata mutation.
- The shared history handler/fold/Lab shell executes 20 genuine domain operations. Coverage is 298 actions and 168 action types. Local tranche review caught the false domain-matrix implementation and the real WebGL reorder refusal; both are fixed without changing expected evidence.
- Current Core v2 tests pass 120 files/1,067 tests; full lint, typecheck, both builds, canonical verification, and packed ESM/CJS consumption pass. The 68-route headless WebGL first/repeat/fresh checkpoint records 830/839 matches per session with the same nine observed and two latent immutable conflicts, canvas cleanup zero, and zero console/page/network/external-fixture errors. Post-review `HIS-001` and `HIS-006` targeted first/repeat/fresh reruns each pass 13/13.
- The latest history 2+7 lifecycle proof covers 5,099 entities with retained-heap median 85,587 bytes and releases DOM, scheduler, renderer, assets, listeners, history, companion state, and pending work. The refreshed transaction-only 2+7 checkpoint is unfavorable versus its predecessor: full-update median/p95 is 21.96/26.29 ms at 100 and 1,118.36/1,921.26 ms at 5,000. Raw samples remain preserved; the change is not attributed without a controlled explanation. WebGPU and native Windows remain pending.

# Next Step

Implement the seven remaining transformer cases `TRN-001/004/005/006/007/008/009` on the existing selection/transformer and root-gesture substrates. Add only the missing transform session semantics, declarative probes, and focused routes; do not create case-specific runtimes where the shared handler/fold suffices. Use targeted product and route tests while building, then one tranche full gate and one local review. Run headless WebGL when the routes are connected; repeat package, memory, or performance only if this tranche changes their exact public, ownership/destroy, or hot-path boundaries. The following tranche is `VIE-005/006/007`.

# Working Boundary

- `src/core-v2/`
- `tests/core-v2/`
- `lab/performance-v2/`
- `scripts/verification/`
- `docs/tasks/2026/07-15/performance-core-v2/`
