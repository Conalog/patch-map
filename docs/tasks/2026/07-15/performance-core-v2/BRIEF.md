# Goal

Connect all 38 approved decisions and 173 cases to PixiJS product behavior, independent actual automation, the same focused Lab route, packed integration, and production-readiness evidence without changing approved expected observations.

# Scope

- Implement all approved records without regenerating, weakening, or editing canonical fixtures, normalized expected evidence, review records, or decision outcomes.
- Work only on `performance/core-v2`; keep Core v1, clean-room/comparison paths, prior evidence, dependency internals, and other refs/worktrees frozen.
- Advance in 5–10 case dependency tranches with shared product/automation substrate and risk-based gates. Prefer headless browser automation; reserve headed runs for behavior that headless cannot establish and final explicit checkpoints.

# Current Facts

- Contract revision `core-v2-functional-contract/2026-07-16.2` contains 173 records, 646 actions, and 1,388 assertions; all journeys are migration-blocking P0.
- Promotion requires expected-blind product execution, independent comparison, the exact focused route, declared packed-host evidence, cleanup, determinism, and review.
- Core v2 accepts PATCH MAP arrays directly, preserves immutable input and stable identity in a dense store, and renders aggregate Mesh/WebGL layers rather than per-entity Pixi objects.
- WebGL2 is mandatory. Chromium 4× is a proxy; WebGPU and native Windows remain pending.

# Current State

- Canonical verification passes 38 decisions, 32 negative probes, and all 173 records unchanged. Thirty-four routes are executable, 33 produce actuals, `DAT-008` truthfully fails at its missing `as`, and 139 remain explicit stubs. The closed diagnostic registry still has the documented immutable-expected conflicts.
- Dense reconcile, affine geometry, aggregate relations, bounded hit indexes, atomic removal, scoped assets, component Sprite lanes, deterministic text, presentation timing, paint order, and engine-level undo/redo are implemented. Semantic bar destinations remain atomic while the presentation controller exposes visible intermediate frames, retarget continuity, deterministic cadence, and destroy-safe clock closure.
- Automation uses shared expected-blind runtimes and detached product probes. The focused Lab exposes actual-only phases and common Run/Repeat/Reset/Destroy controls without importing expected data. Presentation tranche review closed every implementation finding, including surface-domain revision correlation.
- Milestone gates pass 131 files/1,021 unit tests, full lint/typecheck, Core v2/Lab builds, and canonical verification. Headless Chromium 143 passes 19 routes first/repeat/fresh at 280/284 with only three `REN-005` and one `ANI-002` immutable conflicts, real WebGL pixel evidence, canvas maximum one/cleanup zero, and zero console/page/network/external-fixture errors.
- Packed ESM/CJS consumption passes. The 2+7 lifecycle run expands 1,000 items to 5,099 entities, retains a median 135,311 bytes, and releases DOM, scheduler, and renderer ownership. The new full Chromium 4x 2+7 matrix preserves all six scales and reports both favorable and unfavorable Mesh intervals; WebGPU is unavailable and native Windows remains pending.

# Next Step

Implement the eight-case transaction tranche `UPD-001/002/003/004/006/007/008/010`: one staged dense transaction authority, revision-bound element/component handles, strict/permissive atomic bulk updates, complete replacement, relative and center-preserving geometry, component collection reconciliation, and relation refresh. Connect declarative shared handlers/folds plus eight actual-only focused routes. Run targeted tests while building, one tranche-level full unit/lint/typecheck/build/verifier checkpoint, and only the headless browser, packed consumer, memory, or performance gates whose public surface, lifecycle ownership, or measured hot path actually changes. Keep `UPD-009` isolated for the subsequent concurrency tranche.

# Working Boundary

- `src/core-v2/`
- `tests/core-v2/`
- `lab/performance-v2/`
- `scripts/verification/`
- `docs/tasks/2026/07-15/performance-core-v2/`
