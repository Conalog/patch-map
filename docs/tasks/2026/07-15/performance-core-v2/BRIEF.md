# Goal

Connect every approved Core v2 functional-contract decision and case to the PixiJS Core v2 implementation, independent actual-observation automation, the same focused Lab route, packed integration, and release evidence. Completion means 38/38 decisions and all 135 capabilities plus 38 consumer journeys satisfy the immutable expected observations and production-readiness gates.

# Scope

- Implement all 173 approved records without regenerating, weakening, or editing canonical fixtures, normalized expected evidence, review records, or decision outcomes.
- Work only on `performance/core-v2`; keep Core v1, clean-room/comparison paths, prior evidence, dependency internals, and other refs/worktrees frozen.
- Advance in dependency-ordered tranches, pairing each implementation change with independent automation and its unique light-theme Lab route; run aggregate package, browser, memory, performance, security, accessibility, integration, and migration gates only at meaningful milestones.

# Current Facts

- Contract revision `core-v2-functional-contract/2026-07-16.2` contains 173 digest-bound records, 381 action definitions, 646 ordered actions, and 1,388 typed assertions. The capability registry has 83 P0 and 52 P1 cases; all 38 consumer journeys are migration-blocking P0.
- Canonical generation proves specification integrity only. Actual execution, Lab, implementation, integration, and release status start as `not-run`/`unassessed` and must be derived from package-bound actual evidence.
- The existing prototype provides an immutable v0.10 parser, dense store, aggregate Mesh renderer, manual Pixi lifecycle, root interaction, central scheduler, assets, extraction, package entry, and performance harness. It is reusable infrastructure, not evidence of any of the 173 cases by itself.
- WebGL2 is mandatory. Chromium 4× remains a development proxy; WebGPU is optional and native Windows Chrome/Edge, accessibility-device, security, migration, and target-performance evidence remain pending until executed.

# Current State

- Contract baseline `c491c74` contains the approved corpus/verifier. `verify:core-v2-contract` passes 38 decisions, the canonical catalog plus 32 negative drift probes, and all 173 approved records.
- Product commits through `93890a1` add immutable direct-JSON materialization, lifecycle/diagnostics, semantic layout/probes, and PixiJS `Color`. Automation through `b22a6f4` adds six-case product execution, ordered JSON-safe event/evidence capture, distinct `DAT-002` generations, comparison, and 14-domain folds. Actual results are `LIF-001` 11/11, `LIF-002` 18/20, `DAT-001` 7/10, and `DAT-002` 13/13.
- `DAT-003/004/005` have 10 product-injected handlers for all 13 actions; their fold is active. The Lab owns 173 unique routes; its first-six bridge is active while the other 167 remain explicitly `not-implemented`.
- A full expected audit finds 12 out-of-registry diagnostic assertions across 10 cases (`QRY-001`, `LIF-002`, `DAT-001`, `DAT-004`, `DAT-006`, `AST-001`, `AST-002`, `UPD-003`, `UPD-009`, `ANI-002`). Product code keeps the registry closed and actual evidence retains each mismatch pending a versioned contract-owner resolution; unaffected implementation continues.
- `CSM-001` and `CSM-003` execute on the source product seam but remain deliberately non-promotable without a digest-bound packed-host probe. `DAT-001 /scene/orderHash` remains unresolved because the approved contract defines a literal but no computation algorithm; the product does not synthesize it.

# Next Step

Finish the first-six browser Lab bridge and the `DAT-003/004/005` actual fold, then implement the remaining P0 lifecycle/data closure (`LIF-004/005`, `DAT-006/007/008`) before beginning rendering/layout/assets. Preserve every current mismatch and run packed/fresh-session evidence only after the source route is stable.

# Working Boundary

- `src/core-v2/`
- `tests/core-v2/`
- `lab/performance-v2/`
- `scripts/verification/`
- `docs/tasks/2026/07-15/performance-core-v2/`
