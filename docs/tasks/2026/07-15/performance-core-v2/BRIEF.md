# Goal

Connect all 38 approved decisions and 173 cases to PixiJS product behavior, independent actual automation, the same focused Lab route, packed integration, and production-readiness evidence without changing approved expected observations.

# Scope

- Implement all approved records without regenerating, weakening, or editing canonical fixtures, normalized expected evidence, review records, or decision outcomes.
- Work only on `performance/core-v2`; keep Core v1, clean-room/comparison paths, prior evidence, dependency internals, and other refs/worktrees frozen.
- Advance in 5–10 case dependency tranches with shared product/automation substrate and risk-based gates. Prefer headless browser automation; reserve headed runs for behavior that headless cannot establish and final explicit checkpoints.

# Current Facts

- Contract revision `core-v2-functional-contract/2026-07-16.2` contains 173 records, 646 actions, and 1,388 assertions; all journeys are migration-blocking P0.
- Promotion requires expected-blind product execution, independent comparison, the exact focused route, declared packed-host evidence, cleanup, determinism, and review.
- Core v2 accepts PATCH MAP arrays directly, preserves immutable input and stable identity in a dense store, and renders aggregate Mesh/WebGL layers rather than per-entity Pixi objects.
- WebGL2 is mandatory. Chromium 4× is a proxy; WebGPU and native Windows remain pending.

# Current State

- Canonical verification passes 38 decisions, 32 negative probes, and all 173 records unchanged. Forty-seven routes are executable, 46 produce actuals, `DAT-008` truthfully stops at its malformed approved action, and 126 remain explicit stubs.
- `UPD-011/012/013/014` now share one async revision owner, renderer-only presentation sidecar, accepted-versus-visible live-overlay ledger, targeted semantic refresh, expected-blind automation, and the same focused Lab shell. Input, stable identity, selection, history, and persisted data remain detached from transient presentation.
- Core v2 unit tests pass 111 files/950 tests; lint, typecheck, both builds, canonical verification, packed ESM/CJS consumption, and targeted headless WebGL first/repeat/fresh pass. The 32-route checkpoint preserves six observed immutable conflicts, deterministic actuals, canvas cleanup zero, and zero console/page/network/external-fixture errors.
- The refreshed 2+7 lifecycle gate covers 5,099 entities with retained-heap median 87,067 bytes and releases DOM, scheduler, renderer, assets, and pending work. The transaction CPU checkpoint preserves 2 warmups plus 7 samples; the unfavorable 5,000-item p95 spikes remain visible. WebGPU and native Windows remain pending.
- The final Chromium 4x 2+7 matrix preserves 162 measured raw trials across all six scales. Mesh remains the selected production renderer because large-scene full/partial updates dominate Particle, while Particle remains faster in several small-scene and production first-frame/prepare intervals. WebGPU is unavailable and native Windows remains pending.

# Next Step

Implement the seven-case viewport tranche `VIE-001/002/003/004/008` plus `CSM-009/010`. Build one view authority for gesture series, settle/serialization, safe restore fallback, focus/fit contributor rules, resize preservation, and destroy/remount cleanup; connect its declarative actions and probes to one expected-blind runtime and the same focused Lab shell. Use targeted tests while building, then one tranche full unit/lint/typecheck/build/verifier checkpoint. Run headless WebGL because real input and remount behavior changes; run memory or packed gates only if lifecycle ownership or the public consumer seam changes.

# Working Boundary

- `src/core-v2/`
- `tests/core-v2/`
- `lab/performance-v2/`
- `scripts/verification/`
- `docs/tasks/2026/07-15/performance-core-v2/`
