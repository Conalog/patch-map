# Goal

Connect all 38 approved decisions and 173 cases to PixiJS product behavior, independent actual automation, the same focused Lab route, packed integration, and production-readiness evidence without changing approved expected observations.

# Scope

- Implement all approved records without regenerating, weakening, or editing canonical fixtures, normalized expected evidence, review records, or decision outcomes.
- Work only on `performance/core-v2`; keep Core v1, clean-room/comparison paths, prior evidence, dependency internals, and other refs/worktrees frozen.
- Advance in 5–10 case dependency tranches with shared product/automation substrate and risk-based gates. Prefer headless browser automation; reserve headed runs for behavior that headless cannot establish and final explicit checkpoints.

# Current Facts

- Contract revision `core-v2-functional-contract/2026-07-16.2` contains 173 records, 646 actions, and 1,388 assertions; all journeys are migration-blocking P0.
- Promotion requires expected-blind product execution, independent comparison, the exact focused route, declared packed-host evidence, cleanup, determinism, and review.
- Core v2 accepts PATCH MAP arrays directly, preserves immutable input and stable identity in a dense store, and renders aggregate Mesh/WebGL layers rather than per-entity Pixi objects.
- WebGL2 is mandatory. Chromium 4× is a proxy; WebGPU and native Windows remain pending.

# Current State

- Canonical verification passes 38 decisions, 32 negative probes, and all 173 records unchanged. Forty-two routes are executable, 41 produce actuals, `DAT-008` truthfully fails at its missing `as`, and 131 remain explicit stubs. The closed diagnostic registry and every documented immutable-expected conflict remain unchanged.
- The eight-case transaction tranche `UPD-001/002/003/004/006/007/008/010` now uses one staged dense transaction authority. It covers revision-bound targets, merge/replace/relative geometry, validated empty and strict/permissive bulk updates, component collection reconciliation, and relation refresh without per-entity Pixi ownership. Direct dense bar animation marks parser geometry stale until JSON reconciliation so aggregate Mesh vertices and dirty uploads follow the current dense authority.
- One shared expected-blind handler, fold, runtime, and focused-Lab shell serve all eight routes; product/Lab code imports no normalized expected or comparison data. Headless Chromium 143 passes 27 routes first/repeat/fresh at 374/379 per session, preserving five observed immutable conflicts plus two declared latent conflicts, with real WebGL2 draws, canvas cleanup zero, and zero console/page/network/external-fixture errors.
- Milestone gates pass 141 files/1,148 unit tests, full lint/typecheck, Core v2/Lab builds, canonical verification, and packed ESM/CJS consumption. The 2+7 lifecycle run covers 5,099 entities with median retained heap 88,147 bytes and releases DOM, scheduler, renderer, and pending work.
- The final Chromium 4x 2+7 matrix preserves 162 measured raw trials across all six scales. Mesh remains the selected production renderer because large-scene full/partial updates dominate Particle, while Particle remains faster in several small-scene and production first-frame/prepare intervals. WebGPU is unavailable and native Windows remains pending.

# Next Step

Implement the high-risk structural tranche `UPD-009` in isolation: deterministic cross-parent move, group, ungroup, order preservation, world-geometry preservation, relation refresh, selection/host-companion rebinding, one history unit per accepted action, and atomic cycle rejection. Reuse the existing staged transaction, history, geometry, relation, and focused-Lab substrates; add a separate handler only where structure orchestration introduces genuinely new semantics. Run targeted tests while building and one tranche-level full unit/lint/typecheck/build/verifier checkpoint. Re-run headless WebGL because hierarchy publication changes real renderer behavior; re-run memory only if ownership/destroy paths change and performance only if a measured hierarchy hot path changes. After this tranche, group the async/presentation cases around shared latest-revision and host-overlay substrates rather than case-specific runtimes.

# Working Boundary

- `src/core-v2/`
- `tests/core-v2/`
- `lab/performance-v2/`
- `scripts/verification/`
- `docs/tasks/2026/07-15/performance-core-v2/`
