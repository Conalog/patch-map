**2026-07-15**
- Established the Core v2 durable policy, branch-local AGENTS instructions, and compact resume brief after reading the completed Core v1 policy/brief and required project-context/PixiJS skills.
- Completed the official PixiJS v8 API review and documented the direct-v0.10 parser boundary, aggregate layer architecture, two-spike experiment, phase-separated benchmark protocol, selection rule, and lifecycle risks.
- Implemented the immutable direct parser, both aggregate Pixi spikes, manual Application/Prepare renderer, central scheduler, transformed hit/view utilities, BitmapText/CJK fallback, explicit assets/capture/destroy, Core v2 package entry, and raw benchmark harness; the quick 4x checkpoint selected Mesh.
- Completed the final 2+7 Chromium 4x matrix for 100/500/1,000/2,000/5,000 and production with 162 raw trials and zero console, page, or network errors; independent selected Mesh production full-bar p95 is 32.3 ms and partial-bar p95 is 8.5 ms.
- Closed lifecycle and correctness audit gaps: bar-only Mesh uploads preserve structural replacement safety, shared Pixi Assets use global leases, duplicate source IDs fail atomically, Mesh radius/stroke degradation is structured, text changes cannot no-op, and retained heap is sampled after runtime references leave scope.
- Passed final Core v2 headed browser, packed ESM/CJS consumer, build/typecheck/lint/unit, post-GC memory lifecycle, and performance evidence verification; WebGPU is unavailable in this Chromium environment and Windows native remains pending.
- Corrected the production animation checkpoint after proving that the source fixture hides all 9,365 bars: the final protocol now measures a separate visibility commit and actual intermediate/final geometry, and selected Mesh full-bar trial-p95 p95 is 43.5 ms rather than the superseded hidden-bar 32.3 ms.
- Completed the code-cleared 13:52:05Z final matrix after adding explicit unsupported-schema diagnostics, non-target headed proof, and unresolved-asset debug recomputation; selected Mesh records 42.8 ms full and 10.6 ms partial production trial-p95 p95, superseding the earlier 43.5/14.8 checkpoint.

**2026-07-16**
- Adopted the in-worktree Core v2 functional corpus at revision 2026-07-16.2: 38 approved decisions, 135 capabilities, 38 consumer journeys, 381 action definitions, 646 actions, and 1,388 typed assertions remain immutable implementation inputs.
- Re-ran the contract verifier successfully (38/38 decisions, canonical plus 32 negative probes, 173/173 approved) and started dependency-ordered P0 inventory for implementation, package-bound actual observations, and one focused Lab route per case.
- Implemented and committed the first product foundation: strict direct PATCH MAP array materialization with 7 element/4 component kinds, immutable defaults/stable hashing/closed diagnostics (e252007), followed by the CoreV2Engine lifecycle authority with deduplicated initialization, latest-request draw authority, manual frame publication, DPR-aware center-preserving resize, selection reset, and late-allocation destroy cleanup (71f4bd2). Targeted 44 tests, TypeScript, and owned lint pass; T0 executor-safe catalog coverage also passes 173 cases, 381 handlers, and 646 steps while semantic observer/action execution/Lab remain in progress.

**2026-07-16**
- Added independent actual observation, comparison, append-only evidence, and focused-Lab route substrates without importing normalized expected values into the executor or Lab. Evidence requires two fresh sessions, exact package/catalog bindings, cleanup, determinism, and external review before promotion.
- Executed `LIF-001`, `LIF-002`, `DAT-001`, `DAT-002`, `CSM-001`, and `CSM-003` through the real `CoreV2Engine` with an injected test surface. Added ordered ready/scene/draw/frame/diagnostic/destroyed journals, detached dataset/export observations, post-use input audits, and distinct `session:1`/`session:2` generations.
- Compared the folded real-product actuals against immutable normalized expected: `LIF-001` passes 11/11 and `DAT-002` passes 13/13; `LIF-002` passes 18/20 with hidden-render proof unavailable plus the preserved `INVALID_VALUE` mismatch; `DAT-001` passes 7/10 with visible-bounds and undefined order-hash unavailable plus the preserved `INVALID_RECORD_KIND` mismatch.
- Source `CSM-001` passes 8/24 and `CSM-003` passes 9/21. Their remaining host-seam leaves require packed-host evidence; stale-gesture/history-integrity leaves remain unavailable rather than fabricated.
- Implemented PixiJS public-API color resolution and exact DAT-003/004/005 action handlers. Color and grid inputs remain detached, invalid probes publish nothing, and DAT-004 preserves the approved `INVALID_COLOR` versus actual closed-code `INVALID_VALUE` conflicts.

**2026-07-20**
- Reconfirmed the active 173-case Goal, isolated branch/worktree, policy/BRIEF boundary, and canonical verifier: 38 approved decisions, 173 approved cases, and all 32 negative drift probes pass without canonical artifact changes.
- Closed the P0 lifecycle/data execution slice through `LIF-004/005` and `DAT-003..008`. `LIF-004` matches 12/12 and `DAT-007` matches 10/10 after strict PixiJS `Color` validation; `DAT-008` preserves the immutable `retainTarget` action missing its required `as` operand as a real failed execution with completed cleanup.
- Replaced the six-case Lab bridge with one expected-blind 14-case runtime. Independent review found and verified fixes for supplemental WebGL cleanup truth, release counts, and post-failure `not-run` action rows. Forty targeted tests, scoped lint, and the Vite Lab build pass; the large-chunk warning remains recorded.
- Added semantic patch planning, deterministic dense reconcile planning, and `CoreV2.reconcile`. A safe candidate commits exactly one incremental batch, including empty semantic-only commits; same-ID ref/slot/selection survives, while unsupported background or retained-order changes are refused without changing authority.
- Audited all 30 referenced profile datasets and the production-like fixture. Strict materialization now accepts every actually used canonical array field and explicitly supports image opacity, text overflow, rect event mode, and four-corner radius tuples. Remaining parser projection loss/degradation is catalogued for rendering/layout/assets rather than treated as supported.
- Verification at this checkpoint includes 51 targeted product tests, TypeScript, scoped lint, contract verification, and the Lab production build. Commits `b657480`, `73edd21`, and `b81f7e4` keep Lab, reconcile, and schema support as separate intents.

**2026-07-20**
- Connected `CoreV2Engine.patch` to incremental reconcile as the sole semantic mutation writer. Surface commit precedes authority/revision/change publication; unchanged, rejected, refused, or legacy-surface paths publish nothing and never call full load.
- Closed the first rendering/layout contract slice: `LAY-001`, `REN-001..004` now have exact expected-blind handlers, fourteen-domain folds, immutable captures, and the same focused Lab routes. The Lab now has 19 executable cases, 154 explicit stubs, 76 actions, and 45 action types.
- Independent comparison observes 45/49 leaves through injected Node surfaces and names the four unavailable renderer/gesture leaves. Public real-Pixi surface metrics close all 49/49 without importing expected evidence into product execution; a separate browser probe remains a fallback, not a fabricated default.
- Corrected generic executor transport for optional consumed bindings and slash-delimited capture paths. The new rendering automation plus Lab integration passes 69 targeted tests, TypeScript, scoped lint, node syntax checks, and the Core v2 Lab production build.
- Re-ran the existing headed WebGL product Lab: 31 Mesh/Particle/production checks pass with zero console, page, or network errors. The >500 kB Vite chunk warning remains visible and unwaived.
- Inventoried the remaining P0 rendering/layout/assets scope exactly: 11 cases, 51 actions, 28 action types, 190 assertions, and four checkpoints. Selected `LAY-005 -> LAY-004 -> REN-007` as the next affine geometry slice; assets, deterministic animation, international text, and stacking/history remain separate waves.
- Closed `LAY-005` with one signed-affine projection sidecar, exact local/world/screen/visible bounds, transformed hidden/zero-size hit rules, and atomic `CoreV2Engine.destroyTarget` through incremental reconcile. Independent review caught and verified the fix for fabricated legacy revision/local-bounds fallbacks; missing surface facts now remain explicitly unavailable.
- Added expected-blind LAY-005 handlers/fold and independent 14/14 comparison, then connected the same focused Lab route. The Lab now has 20 executable cases, 153 explicit stubs, 80 actions, and 48 action types; route/execution tests, TypeScript, scoped lint, and the Core v2 Lab build pass with the existing large-chunk warning still visible.
- Extended the stdout-only headed checkpoint to six rendering routes. Chromium first/repeat comparisons pass 63/63 with stable per-case digests, one transient canvas returning to zero, and zero console, page, or network errors; canonical verification still passes 38 decisions, 32 negative probes, and 173 approved records. The next geometry dependency is `LAY-004 -> REN-007`.
- Closed `LAY-004` with one signed affine projection across parser, dense reconcile, Mesh, Particle/Graphics, Pixi leaf matrices, Engine geometry probes, and inverse view math. Follow-item content preserves F×R signed bases; upright content keeps screen identity and stable centers without per-entity item containers.
- Added expected-blind orientation execution and an independent 11/11 comparison, then connected the same focused Lab route. The Lab now has 21 executable cases, 152 explicit stubs, 84 actions, and 52 action types; route/execution tests, TypeScript, lint, and the production Lab build pass.
- Actual browser execution exposed an IEEE negative-zero basis value that Node comparisons had tolerated. The affine basis now canonicalizes signed zero with a direct regression test; both headless and actual headed Chromium subsequently pass seven routes first/repeat 74/74 with stable digests, transient canvas maximum one/cleanup zero, and zero console, page, or network errors.
- The remaining P0 render/layout/assets inventory is 9 cases, 43 actions, 20 action types, 165 assertions, and three checkpoints. `REN-007` is in progress as the relation-path and transformed-hit consumer of the completed affine source.

**2026-07-20**
- Closed `REN-007` with collision-safe ordered relation identity, deterministic self-links and pair deduplication, endpoint omission/visibility, atomic structural link reconcile, and aggregate Mesh plus Particle/Graphics paths driven by the shared signed-affine resolver. Cross-chunk adjacency propagates endpoint dirty ranges without an O(N²) topology build.
- Added one root-level relation hit API over cached geometry. Per-segment DDA cells, bounded memberships, overflow handling, exact stroke-aware narrow phase, and topmost merge avoid an entity/listener hot path; the entity hit path also gained a bounded spatial broad phase and commit-time cleanup that scales with touched identities rather than all stale IDs.
- Added exact expected-blind `REN-007` execution/folding and the same focused Lab route. Six real actions produce 26/26 independent observations, immutable input fingerprints, deterministic fresh sessions, and two-engine cleanup. The Lab now has 22 executable routes, 151 explicit stubs, 90 actions, and 55 action types.
- The first headed relation checkpoint correctly failed because the main and fresh-repeat engines temporarily owned two canvases. Sequential handoff now keeps the repeat engine authoritative through capture and cleanup; the rerun passes eight routes first/repeat 100/100 with deterministic digests, canvas initial/max/cleanup/destroy counts `0/1/0/0`, and zero console, page, or network errors.
- Product, automation, Lab, performance correction, browser serialization, and headed verification are isolated in commits `1283179`, `622feda`, `ad0250c`, `d78d96c`, `c238401`, and `e89f495`. Full Core v2 tests pass 54 files/410 tests; typecheck, lint, both builds, and canonical verification pass. The existing unwaived >500 kB Vite warning and Windows-native pending status remain visible.
- Re-inventoried the remaining P0 rendering/layout/assets slice as 8 cases, 37 actions, 17 distinct action types, 139 assertions, and three checkpoints. Dependency order is `AST-001 -> REN-005 -> REN-010/008 -> REN-006/011 -> REN-009 -> LAY-003`; asset lifecycle is next.
- Closed AST-001 product lifecycle with immutable descriptor normalization, exact builtin catalog, per-engine sessions, backend-wide deduplication, required-asset preflight, scoped leases, pending cancellation, cleanup-failed quarantine, late-surface ownership, and unique physical reload generations. Product and memory-policy commits are a8c7cfa and ddfebee.
- Added expected-blind AST-001 handlers and independent fold, then connected the same focused Lab route. Eight actions produce 17/18 immutable comparisons; only expected ASSET_ALIAS_CONFLICT versus closed-registry actual CONFLICT remains, so the route reports observed-contract-conflict. Commits are c00b44c and 2595cd5.
- Headless and actual headed Chromium pass first, repeat, and fresh deterministic runs with one transient canvas returning to zero and zero console, page, network, or prohibited asset requests. The stdout-only checkpoint is cef0778; cumulative executable coverage is 23 routes with 150 explicit stubs.
- Final verification passes 91 files and 686 unit tests, typecheck, lint, canonical 38-decision and 173-case verification, Core v2 build, packed ESM/CJS consumer, and the 2+7 memory gate. The memory run expands 1,000 items to 5,099 entities, retains a median 83,435 bytes, and releases DOM, canvas, scheduler, and renderer; frozen result artifacts were restored unchanged.

**2026-07-20**
- Closed `REN-005` product behavior with lossless authored image sources, stable dense/component identity, exact four-matrix intrinsic projection, aggregate Pixi Sprite/placeholder rendering, source-generation race suppression, bounded target attempt history, frame-safe unload, and public image/request/lifecycle probes. Product commit is `41fcb96`.
- Added four expected-blind actions, a fourteen-domain fold, exact capture and post-destroy cleanup cross-links, and the same focused Lab route. All 28 actual observations are produced; independent comparison reports exactly 25 passes plus three immutable parent-object `VALUE_MISMATCH` conflicts. Runtime commit is `5f65b2e`; focused click-only Lab/headed verification is `b3eebef`.
- Independent review identified repeated image `setChildIndex` moves as a 5,000-object adversarial risk. Replaced them with one public detach plus 1,024-child public append batches and added deterministic reverse/random tie stress coverage. Leaf tests pass 21/21; the performance correction is `95867b9`.
- Final REN-005 milestone verification passes 99 files/757 unit tests, full lint, typecheck, canonical 38-decision/32-negative/173-record verification, Core v2 and Lab builds, packed ESM/CJS consumption, and 2+7 lifecycle memory. The current observational memory run covers 5,099 entities with median 94,475 retained JS-heap bytes and zero lifecycle failures; its generated result file was restored to the frozen baseline.
- Actual headed Chromium 143 on Darwin passes nine routes first/repeat/fresh at 125/128, with only the three declared REN-005 conflicts, stable REN-005 digest `5f8c245bd63e4149cf0dd2706d2127960fd1c0667e3338809c234665dba0f2a0`, canvas `0 -> 1 -> 0`, and zero console/page/network errors. Per-run RAF/Long Tasks values remain observational development telemetry; Chromium 4x and native Windows performance claims are not inferred from them.
- Executable coverage is now 24 routes, 23 actual-producing routes, 149 explicit stubs, 102 actions, and 62 action types. The next product tranche is `REN-008` item backgrounds plus `REN-010` icons; six rendering/layout/assets cases, 25 actions, 10 action types, and 93 assertions remain after REN-005.

**2026-07-20**
- Closed `REN-008` and `REN-010` with stable owner-qualified component projections, full-item background authority, separate background/content asset lanes, real aggregate Mesh/Graphics plus Sprite paint probes, source-generation correlation, frame-safe release, visibility restoration, and semantic/renderer tint agreement. Background aggregate geometry deliberately reports zero per-component render objects while retaining one visible primitive.
- Added seven expected-blind actions and a shared component-asset fold/runtime. Two fresh executions per case match `REN-008` 10/10 and `REN-010` 11/11, preserve input and component/dense identity, use no supplemental WebGL lease or network fetch, and drain runtime/backend/controller ownership to zero. The implementation and execution stack is `3b0dfcc`, `5e4ddf0`, `4643b6a`, `9061b8d`, `608f707`, `071cea7`, `baa8d4e`, and `684e7b9`.
- Added actual-only four-phase background and three-phase icon inspectors with resource journals and RAF/Long Tasks telemetry. Headless and actual headed Chromium 143 on Darwin pass eleven routes first/repeat/fresh at 146/149; only the three frozen REN-005 parent-object conflicts remain. REN-008 is 10/10 and REN-010 is 11/11, canvas is `0 -> 1 -> 0`, and console/page/network/external-fixture errors are zero. Lab/browser commits are `ce1fc1e` and `1be96b0`.
- Milestone verification passes 106 files/794 unit tests, typecheck, full lint, canonical 38-decision/32-negative/173-record verification, Core v2 and focused-Lab builds, packed ESM/CJS consumption, and the 2+7 lifecycle memory gate. The observational memory run covers 5,099 entities with median 106,351 retained JS-heap bytes and zero DOM/scheduler/renderer retention; generated outputs and frozen evidence remain unmodified.
- Re-inventoried the remaining rendering/layout/assets work as four cases, 18 actions, and 72 assertions. `REN-006/011` is next and requires a pinned deterministic Unicode layout sidecar rather than current code-point/Pixi measurements; `REN-009` animation and `LAY-003` stacking/history follow. WebGPU and Windows-native results remain pending.

**2026-07-20**
- Closed `REN-006` and `REN-011` with exact-source grapheme/bidi semantics, pinned fallback and layout metrics, bounded split/wrap/overflow/auto-font work, upright affine cancellation, BitmapText/guarded Text routing, and frame-correlated renderer publication. Product commits are `b9077a0`, `23e84bb`, `6f71959`, `10766aa`, `1775e33`, `60b4583`, and `26b356a`.
- Added a shared expected-blind text runtime, declarative case registration, detached pre-load authored-fact oracle, export-fidelity poison tests, and failed-run product-resource cleanup. `REN-006` matches 30/30 and `REN-011` matches 20/20 in repeat and fresh sessions; commits are `ae696e4` and `aef31d1`.
- Added actual-only six-phase and seven-specimen focused inspectors plus a 13-route browser checkpoint in `baa8b1f`. Chromium 143 first/repeat/fresh passes 196/199 with only the three declared `REN-005` conflicts, deterministic digests, canvas maximum one and cleanup zero, and zero console/page/network/external-fixture errors. Independent tranche review is clean.
- Milestone verification passes 118 files/932 unit tests, full lint/typecheck, Core v2/Lab builds, and canonical contract verification; the final fold-only oracle correction passes 8 targeted files/119 tests. Package, memory, and performance gates were intentionally not repeated because package exports, resource ownership, destroy paths, and renderer hot paths were unchanged. The next six-case tranche owns placement, presentation timing, animation, paint order, and history.

**2026-07-20**
- Closed the six-case presentation tranche `LAY-002/003`, `UPD-005`, `REN-009`, and `ANI-001/002`. Pure placement, stable paint order, deterministic presentation timing, direct-bar animation, retarget continuity, frame cadence, destroy closure, surface-domain revision correlation, and engine undo/redo are isolated in `ccb7931`, `117293d`, and `eccfd87`.
- Shared expected-blind layout/dynamics runtimes and six actual-only Lab routes raise coverage to 34 executable cases, 139 stubs, 145 actions, and 74 action types. Independent review findings were fixed and the narrow rejudgment closed the final geometry-domain mismatch.
- Full gates pass 131 files/1,021 unit tests, lint, typecheck, Core v2/Lab builds, and canonical verification. Headless Chromium 143 passes 19 routes first/repeat/fresh at 280/284; the only mismatches are three declared `REN-005` parent-object conflicts and the declared `ANI-002` closed-registry conflict. Real WebGL pixels, one-canvas ownership, cleanup zero, and all console/page/network counters pass.
- Packed ESM/CJS consumption and 2+7 lifecycle memory pass; the latter covers 5,099 entities with median retained heap 135,311 bytes and releases DOM, scheduler, and renderer ownership. Generated pointer files were restored to their frozen versions.
- The final 2+7 Chromium 4x matrix passes 18 run groups/162 trials with six scales, zero browser errors, and raw sample/statistic preservation in `full-4x-2026-07-21T07-59-41-973Z`. Selected Mesh full/partial animation p95 and selection beat Particle at every scale, but the recorded unfavorable intervals include production GPU prepare `213.7 > 13.1` ms, first frame `380.6 > 356.0` ms, pan `3.7 > 2.5` ms, and destroy `198.4 > 164.3` ms. Evidence commit is `6ba94dd`; WebGPU is unavailable and Windows native remains pending.

**2026-07-23**
- Closed `UPD-001/002/003/004/006/007/008/010` on one staged transaction authority. Strict and permissive target sets, validated empty bulk no-op, detached merge/replace, relative and center-preserving geometry, component reconciliation, relation refresh, stable identity, input immutability, atomic refusal, history, and publication ordering are covered in product commit `86e6a46`.
- Connected all eight cases through one expected-blind handler/fold/runtime and the same actual-only focused Lab in `dae3b18`. Coverage is 42 executable routes and 131 explicit stubs; no product, handler, runtime, or Lab path imports normalized expected/comparison data.
- Headless Chromium 143 passes 27 WebGL routes first/repeat/fresh at 374/379 per session. Five immutable conflicts remain observed and two remain declared latent; canvas cleanup is zero, real WebGL2 draw evidence is present, and console/page/network/external-fixture error counts are zero. All owned browser/server processes are closed after the checkpoint.
- Full verification passes 141 files/1,148 unit tests, lint, typecheck, both builds, canonical 38-decision/32-negative/173-record verification, packed ESM/CJS consumption, and 2+7 lifecycle memory. The memory run covers 5,099 entities with median retained heap 88,147 bytes and releases DOM, scheduler, renderer, assets, and pending work.
- Performance evidence `da11690` preserves a transaction-only 2-warmup/7-sample CPU checkpoint plus a complete six-scale Chromium 4x 2+7 matrix with 162 measured raw trials. Mesh wins the large update path, including 5,000-item full-animation trial p95 `329.5 ms` versus Particle `5,669.1 ms`, while Particle remains faster for several first-frame/prepare intervals including production first frame `474.4 ms` versus Mesh `626.2 ms`. WebGPU is unavailable and native Windows remains pending.

**2026-07-23**
- Closed structural update case `UPD-009` through the existing staged transaction authority. Product commit `acdf277` implements deterministic move/group/ungroup, world-transform and order preservation, logical selection/history companions, aggregate dense reconciliation, input immutability, and atomic cycle/locked-hierarchy refusal; `b7a5dff` adds explicit string-ID guards and complete surface-test doubles.
- Connected the approved seven-action trace through the shared expected-blind update handler/fold/runtime and the same focused Lab route in `59eeccc`. Coverage is 43 executable routes, 42 actual-producing routes, and 130 explicit stubs. Actual cycle code `CONFLICT` remains an honest declared mismatch with immutable expected rather than an alias.
- Full gates pass 141 files/1,158 unit tests, lint, typecheck, Core v2/Lab builds, and canonical 38-decision/32-negative/173-record verification. Headless Chromium 143 passes 28 routes first/repeat/fresh at 387/393 per session, with six observed and two latent declared conflicts, real WebGL2 structural draws, deterministic actuals, canvas cleanup zero, and zero console/page/network/external-fixture errors.
- Packed verification commit `7a1d41b` proves ESM hierarchy transactions, CJS exports, direct-input immutability, selection/history, zero-revision cycle rejection, relation revision correlation, capture, and terminal destroy cleanup. Every owned Node/Vite/Chromium process is absent after both packed and full-route runs. The 2+7 memory and performance matrices were intentionally not repeated because no renderer/resource ownership, destroy path, or measured hot path changed; WebGPU and native Windows remain pending.
- Closed the four-case async/presentation tranche UPD-011/012/013/014 in product commit 881c0f4, shared expected-blind automation/Lab commit d8517b4, and package/memory/performance evidence commit d1305be. Latest-revision dataset ownership releases every temporary resource once, host presentation stays outside persisted data, live overlays coalesce accepted revisions into one visible publication, and targeted semantic refresh preserves IDs, selection, history, and data. Core v2 unit passes 111 files/950 tests; lint, typecheck, Core v2/Lab builds, canonical verification, packed ESM/CJS, targeted headless first/repeat/fresh, and 2+7 lifecycle memory pass. The memory run covers 5,099 entities with 87,067-byte median retained heap and zero DOM/scheduler/renderer retention; the five-scale 2+7 transaction evidence keeps unfavorable 5,000-item p95 spikes visible. All owned browser/server processes terminate; WebGPU and native Windows remain pending.

**2026-07-23**
- Closed `VIE-001/002/003/004/008` and `CSM-009/010` with one Engine viewport authority, policy/focus/fit/restore/settle persistence, transformed root hit testing, cooperative private candidate loads, atomic publication, and renderer lease transfer in product commit `003992f`.
- Connected the seven cases through one shared expected-blind viewport handler/fold/runtime and the same focused Lab route in `04b5903`. Trusted pointer and wheel input use explicit arm, settled, actual-observation, and released milestones; coverage is 54 executable routes, 53 actual-producing routes, and 119 explicit stubs.
- Full Core v2 tests pass 116 files/982 tests together with lint, typecheck, Core v2/Lab builds, and canonical 38-decision/32-negative/173-record verification. Headless Chromium passes 39 routes first/repeat/fresh at 526/532 per session with exactly six approved immutable conflicts, stable actuals, one transient canvas returning to zero, and zero console/page/network/external-fixture errors.
- Packed ESM/CJS consumption and 2+7 lifecycle memory pass. The lifecycle run covers 5,099 entities with median retained heap 86,059 bytes and releases DOM, scheduler, renderer, assets, listeners, and pending work; all owned Vitest/Vite/Chromium processes are absent after verification.
- Evidence commit `da78668` preserves the quick Chromium 4× 2-warmup/7-sample checkpoint. Particle retains small-scene first-frame, text, and heap wins, while selected Mesh remains materially stronger at 1,000 records and full animation. WebGPU is unavailable and native Windows remains pending.

**2026-07-23**
- Closed `QRY-001/002` and `SEL-001/002/003/004` with an immutable logical scene index, owner-qualified component identity, lifecycle/revision-bound query handles, ordered selection-set operations, hierarchy/lock filtering, transformed point hit, relation tolerance, and selection-unit drill resolution in product commit `3d8ceec`.
- Connected all six cases through one shared expected-blind handler/fold/runtime and the same focused Lab controls in `68bb952`. Coverage is 60 executable routes, 59 actual-producing routes, 113 explicit stubs, 236 actions, and 127 action types; `DAT-008` still stops atomically at its immutable missing binding operand.
- Full Core v2 tests pass 117 files/998 tests with full lint, typecheck, Core v2/Lab builds, and canonical 38-decision/32-negative/173-record verification. Headless Chromium 143 passes 45 routes first/repeat/fresh at 580/587 per session with seven observed and two latent declared immutable conflicts, deterministic actuals, canvas cleanup zero, and zero console/page/network/external-fixture errors.
- Tranche review replaced the unconstrained geometry-wide point scan with the existing bounded Core spatial index and compiled selection eligibility once per constrained operation. Targeted SEL-001/003 first/repeat/fresh regressions remain exact after the correction. Per the user-requested agent shutdown, this was a local tranche review; the final independent release review remains pending.
- Packed ESM/CJS browser consumption and the 2+7 lifecycle gate pass in evidence commit `045ca96`. The memory run covers 5,099 entities with median retained heap 87,963 bytes and releases DOM, scheduler, renderer, assets, listeners, and pending work. All processes owned by this worktree's Vitest/Vite/Chromium/package/memory runs are absent; unrelated processes were not touched. Performance matrices were not repeated because no renderer hot path changed. WebGPU and native Windows remain pending.

**2026-07-23**
- Closed `EVT-001/002/003/004/008` and `SEL-005/006` with one root-only pointer authority. It normalizes pointer metadata, click/drag ordering and count, hover exit, interruption, view-revision suppression, box/paint selection, eligibility filtering, and relation intersections without per-entity listeners or callbacks.
- Connected all seven cases through one expected-blind handler/fold/runtime and the same focused Lab controls. Coverage is 67 executable routes, 66 actual-producing routes, 106 explicit stubs, 247 actions, and 136 action types; pointer journaling is scoped to these cases so the six-event legacy executor boundary remains stable elsewhere.
- Local tranche review caught and fixed two cross-slice regressions: universal `pointerEvent` journal subscriptions broke legacy/fake Engines, and `PixiEngineSurface` assumed the new pointer bridge existed on legacy geometry probes. The user-requested agent shutdown remains in force, so final independent release review is still pending.
- Headless Chromium 143 passes 52 WebGL routes first/repeat/fresh at 668/677 per session, with nine observed and two latent declared conflicts, deterministic actuals, canvas cleanup zero, and zero console/page/network/external-fixture errors. Trusted EVT-003 input proves hover enter/leave; EVT-008 proves owned context-menu prevention and unowned pass-through with five federated plus three native root listeners.
- Two new immutable conflicts remain explicit: EVT-003 requests `rect-b` at a point its approved fixture places over `item-a`, and EVT-008 simultaneously requires a parent click-count object without `secondary-unowned` and a child value of zero. Expected evidence and comparison gates remain unchanged.
- Full gates pass 150 files/1,245 unit tests, lint, typecheck, Core v2/Lab builds, and canonical 38-decision/32-negative/173-record verification. Packed ESM/CJS consumption directly exercises pointer and region-selection exports. The 2+7 lifecycle gate covers 5,099 entities with median retained heap 82,995 bytes and releases DOM, scheduler, renderer, assets, listeners, and pending work.
- All owned Vitest/Vite/Chromium/package/memory processes are absent after verification. Performance matrices were not repeated because aggregate render/upload hot paths did not change; WebGPU and native Windows remain pending.

**2026-07-23**
- Closed `EVT-005/006/007/009` and `SEL-008` with one logical host-interaction authority. Stable query bindings, component-owner-surface propagation, ordered specific/family observers, keyboard ownership, transformer isolation, nested interaction modes, canvas-only host selection publication, external selection, scene replacement, remount, and destroy cleanup are isolated in product commits `c5fe35b` and `728e0b8`.
- Connected all five cases through the existing shared expected-blind pointer/selection runtime, handler, fold, and focused Lab shell in `795255c`; no case-specific runtime files or expected-data imports were added. Coverage is 72 executable routes, 71 actual-producing routes, 101 explicit stubs, 262 actions, and 149 action types.
- Full gates pass 151 files/1,260 unit tests, lint, typecheck, Core v2/Lab builds, and canonical 38-decision/32-negative/173-record verification. Headless Chromium 143 passes 57 WebGL routes first/repeat/fresh at 723/732 per session with the same nine observed and two latent immutable conflicts, deterministic actuals, canvas cleanup zero, and zero console/page/network/external-fixture errors.
- Packed ESM/CJS consumption and the refreshed 2+7 lifecycle gate pass in `accff69`. Package probes execute the host-interaction exports and Engine APIs directly; memory covers 5,099 entities with median retained heap 87,483 bytes and releases logical bindings, observers, host callbacks, modes, DOM, scheduler, renderer, assets, and pending work.
- All owned Vite/Chromium/package/memory processes are absent after verification. Performance was intentionally not repeated because aggregate render/upload hot paths did not change. Per the user-requested agent shutdown, final independent release review remains pending; WebGPU and native Windows remain pending.

**2026-07-24**
- Closed `SEL-007/009` and `TRN-002/003/010` in `3dc13bc` with one logical selection/transformer substrate and aggregate Pixi overlay. Oriented single frames, multi-target unions, CSS-stable handles/strokes, transformability filtering, current-ID relation endpoint resolution, handle priority, root gesture isolation, pointer cross-talk suppression, and destroy interruption retain no per-entity renderer or listener authority.
- Connected the five approved traces through the existing shared expected-blind pointer/selection handler, fold, runtime, and focused Lab shell in `6cbbd26`. Browser lifecycle registration is `728c3df`; `5cd787f` corrects verifier assertion totals to the immutable corpus without changing expected evidence. Coverage is 77 executable routes, 76 actual-producing routes, 96 explicit stubs, 275 actions, and 158 action types.
- Full gates pass 152 files/1,276 unit tests, lint, typecheck, Core v2/Lab builds, and canonical 38-decision/32-negative/173-record verification. Headless Chromium 143 passes 62 WebGL routes first/repeat/fresh at 768/777 per session, preserving the same nine observed and two latent immutable conflicts, deterministic actuals, canvas cleanup zero, and zero console/page/network/external-fixture errors. The five new routes match all 45 assertions.
- Package/memory gate hardening is `9a15911`: packed ESM/CJS directly exercises the selection-transformer export, aggregate visual probe, routing, and completion; active-gesture 2+7 lifecycle covers 5,099 entities with median retained heap 76,939 bytes and releases gesture, pointer capture, host callbacks, DOM, scheduler, renderer, assets, and pending work.
- Quick Chromium 4× evidence `6dadb23` preserves 2 warmups and 7 measured samples at 100/1,000 records. Selected Mesh selection is 1.9/7.3 ms; Mesh spike full-animation p95 is 2.2/29.3 ms versus Particle 20.8/135.8 ms. Particle remains favorable for 100-record first frame (37.7 vs 48.7 ms), 1,000-record GPU prepare (8.6 vs 23.4 ms), and retained heap. Large improvements versus the prior local checkpoint are treated as run/environment variability, not attributed to this tranche.
- All owned Vitest/Vite/Chromium/package/memory/performance processes are absent after verification. The user-requested agent shutdown remains in force, so this is a local tranche review and final independent release review remains pending. WebGPU is unavailable and native Windows remains pending. The next tranche is `HIS-001..006`, followed by `TRN-001/004..009` and `VIE-005/006/007`.

**2026-07-24**
- Closed `HIS-001..006` with one bounded Engine semantic-history authority in `facb805`, shared expected-blind execution/fold/Lab routes in `1b02a4a`, package/lifecycle proof in `94e99e0`, and the initial transaction checkpoint in `f885b78`. Coverage is 83 executable routes, 82 actual-producing routes, 90 explicit stubs, 298 actions, and 168 action types.
- Local tranche review rejected a count-only domain matrix that used metadata mutations for unrelated semantics. `6f9bbe3` adds immutable structural `add`, exact grouped-command element/component order restore, and real create/duplicate/reorder/group/ungroup/delete behavior; `c4542f7` makes all 20 history domain rows observable and independently checked. Immutable expected evidence and comparison rules are unchanged.
- Full Core v2 verification passes 120 files/1,067 tests, full lint, typecheck, Core v2/Lab builds, and canonical 38-decision/32-negative/173-record verification. The full 68-route headless checkpoint records 830/839 matches per first/repeat/fresh session with the same nine observed and two latent conflicts. Post-fix `HIS-001` and `HIS-006` each pass 13/13 in targeted first/repeat/fresh real-WebGL reruns; canvas cleanup and all browser error counters are zero.
- Packed ESM/CJS proof `b1e298e` exercises structural add plus undo/redo and finishes lifecycle-clean. The existing history 2+7 ownership proof remains applicable because the review fix did not change renderer/resource/destroy ownership; it covers 5,099 entities with retained-heap median 85,587 bytes and terminal release of history/companion/DOM/scheduler/renderer state.
- The required hot-path refresh `5ffc8d4` preserves two warmups and seven samples at 100/500/1,000/2,000/5,000. It is materially slower than the preceding local run, including full-update median/p95 `21.96/26.29 ms` at 100 and `1,118.36/1,921.26 ms` at 5,000; the unfavorable raw result is retained rather than rerun until favorable. No causal attribution is made from this uncontrolled delta.
- All processes owned by this worktree's headless browser, package, unit, and performance runs are absent. Remaining Chromium processes were parented by unrelated worktrees or the shared gstack browse service and were not terminated. No child agents are active; final independent release review, WebGPU, and native Windows remain pending.

**2026-07-24**
- Closed `TRN-001/004/005/006/007/008/009` with immutable move/resize/rotate planning and one Engine preview/session authority in `175a1da`. Aggregate surface previews remain outside semantic/history authority; completion produces one transaction, while selection, replace, redraw, undo/redo, and destroy cancel ownership without stale completion.
- Connected all seven traces through the existing expected-blind pointer/selection handler, fold, runtime, and focused Lab shell in `b43f420`. Coverage is 90 executable routes, 89 actual-producing routes, 83 explicit stubs, 318 actions, and 184 action types; no case-specific runtime or expected-data import was added.
- Full gates pass 122 files/1,091 Core v2 tests, lint, typecheck, Core v2/Lab builds, and canonical 38-decision/32-negative/173-record verification. Targeted headless Chromium 143 first/repeat/fresh passes all seven new routes and 77/77 assertions per session with deterministic actuals, canvas cleanup zero, and zero console/page/network/external-fixture errors. The unchanged 68-route full checkpoint was not repeated.
- Packed and lifecycle proof `5dcbe49` exercises ESM/CJS planner, snap, session preview/cancel, and destroy cleanup. The 2+7 run covers 5,099 entities with retained-heap median/p95 83,811/403,051 bytes; every live preview, overlay, capture, DOM, scheduler, renderer, asset, listener, history, and pending-work owner is released.
- Local tranche review removed fixture-only history pollution and reran the affected `TRN-006` unit and headless route. No child agents are active. All owned Vitest/Vite/Chromium/package/memory processes are absent; performance was not repeated because the existing matrix does not exercise transformer gestures. Final independent release review, WebGPU, and native Windows remain pending. Next is `VIE-005/006/007`.

**2026-07-24**
- Closed `VIE-005/006/007` with exact authored-angle observability, centered rotation/flip transforms, transformed hit/focus/fit/transformer checks, DPR-aware surface resize correlation, and post-resize black-frame accounting in product commit `77f882c`.
- Connected all three approved traces through the existing expected-blind viewport handler, fold, runtime, and focused Lab shell in `3cccd64`. Coverage is 93 executable routes, 92 actual-producing routes, 80 explicit stubs, 323 actions, and 189 action types.
- Full gates pass 122 files/1,098 Core v2 tests, lint, typecheck, Core v2/Lab builds, canonical 38-decision/32-negative/173-record verification, and packed ESM/CJS consumption. Targeted headless Chromium 143 first/repeat/fresh passes all three routes and 25/25 assertions per session with deterministic actuals, canvas cleanup zero, and zero console/page/network/external-fixture errors.
- Local review added a positive regression for black-frame detection after first proving the real WebGL routes remain at zero. The unchanged 68-route full checkpoint was not repeated. Memory and performance matrices were not repeated because the viewport probe adds scalar observations but no resource ownership, destroy path, or aggregate render/upload hot-path work.
- No child agents are active and all processes owned by the targeted browser/package runs are absent. Final independent release review, WebGPU, and native Windows remain pending. Next is the shared replacement/recovery tranche `LIF-003`, `ERR-002`, `ERR-005`, `CSM-002`, `CSM-004`, and `CSM-037`.
- Checkpointed the scene-replacement/retry and transient panel-fill product substrate in a743c2f and c88b116. Candidate validation now precedes publication, invalid and stale async work remain distinguishable, successful replacement clears only scene-bound logical bindings, and packed RGBA panel fill stays transient while reaching both aggregate Mesh and Particle paths. The final targeted six-file suite passes 43/43 with scoped lint and typecheck; executable/actual/stub coverage remains 93/92/80 because shared expected-blind automation and the six focused Lab routes are intentionally the restart point. Full, browser, package, memory, and performance gates were deferred until that coherent tranche is connected.
- Closed the shared replacement and recovery tranche for ERR-002, ERR-005, LIF-003, CSM-002, CSM-004, and CSM-037 with one expected-blind handler, fold, product runtime, and six focused Lab routes. Successful replacement now proves stale identity, selection, binding, animation, history, and overlay cleanup; declared-failure generations remain isolated on detached hosts so the visible Lab surface owns one Pixi canvas. Final evidence passes 122 files and 1,115 Core v2 tests, lint, typecheck, both builds, canonical 38-decision and 173-case verification, an 84-route headless first/repeat/fresh checkpoint at 1,031 of 1,040 matches with the same nine observed and two latent immutable conflicts, packed ESM/CJS consumption, and 2+7 lifecycle memory at 5,099 entities with retained-heap median/p95 79,375/406,615 bytes. Browser and lifecycle error counters are zero, every owned browser/server/test process is closed, and performance was not repeated because the measured hot path did not change. Coverage is 99 executable, 98 actual-producing, and 74 stub routes; WebGPU, native Windows, and final independent release review remain pending.

**2026-07-24**
- Closed the shared lifecycle/interruption tranche `ERR-004`, `ERR-006`, `PRF-007`, `CSM-017`, and `CSM-036`. Product commit `0d58699` completes transformer edits from root pointer terminals: `up`/`up-outside` commits once, while `cancel`/`leave` reverts and releases gesture ownership without stale history or capture.
- Automation commit `82b83e4` adds one expected-blind product adapter, handler, fold, and focused Lab registration for all five cases. Coverage is 104 executable routes, 103 actual-producing routes, 69 explicit stubs, 376 executable actions, and 210 action types; immutable expected, fixtures, and comparison code remain outside product execution.
- `PRF-007` runs ten production-shaped initialize/load/interact/destroy cycles. Review found that measuring inside the action retained executor evidence and overstated heap growth; the corrected probe follows the established lifecycle method by forcing GC after executor cleanup and cached-input release. Raw heap samples remain only in the approved volatile timing domain, and the compared post-destroy growth is neither aliased nor threshold-clamped.
- Core v2 gates pass 122 files/1,126 tests, full lint, typecheck, both builds, and canonical 38-decision/32-negative/173-record verification. Targeted headless Chromium 143 first/repeat/fresh passes all five routes and 68/68 assertions per session with deterministic digests, canvas cleanup zero, and zero console/page/network/external-fixture errors.
- The prior packed consumer, 2+7 lifecycle, full 84-route browser checkpoint, and unfavorable transaction performance result remain current and were not repeated because package exports, Pixi renderer/resource ownership, destroy authority, and measured hot paths did not change. No child agents are active; all browser/Vite/test processes owned by this checkpoint are closed. Final independent release review, WebGPU, and native Windows remain pending.

**2026-07-24**
- Checkpointed the export/extraction product substrate in `ce61c67`. `CoreV2Engine` now captures only an exact published tuple through Pixi's renderer extraction boundary, retains the original authoritative canvas, exposes CSS/backing dimensions, reports zero Core-owned temporary image/render-texture retention, rejects stale or replaced surfaces, and drains pending work on every terminal path.
- Scoped lint, typecheck, and 15 extraction/lifecycle tests pass. Contract automation, focused Lab routes, packed consumption, memory, browser, and performance gates were intentionally deferred until the coherent `DET-004`/`PIX-004`/`PRF-008`/`CSM-035`/`CSM-038` tranche is connected.

**2026-07-24**
- Connected `DET-004`, `PIX-004`, `PRF-008`, `CSM-035`, and `CSM-038` in `7b3fda1` through one expected-blind export/extraction runtime, shared action handler/fold, and five focused Lab routes. Coverage is 109 executable routes, 108 actual-producing routes, 64 explicit stubs, 396 actions, and 223 action types.
- Current headless Chromium first/repeat/fresh runs match all 70 assertions with one authoritative canvas, exact restore/replacement handoff, data-URL and temporary-image cleanup, and zero console/page/network/external-fixture errors. The browser check exposed and verified corrections for the fresh-instance canvas handoff and approved volatile runtime-resource observation path.
- Scoped lint, three targeted files/206 tests, typecheck, and canonical 38-decision/32-negative/173-record verification pass. Per the user-requested restart, full tranche promotion gates remain the restart point: full unit/lint/build, 94-route headless browser, packed consumer, 2+7 memory, and extraction performance. No child agent or worktree-owned browser/Vite/test process remains active.
- Promoted the export/extraction tranche through 123 files/1,138 Core v2 tests, full lint/typecheck, Core v2 and Lab builds, and canonical 38-decision/32-negative/173-record verification. The existing large-chunk warning remains visible and unwaived.
- Headless Chromium 143 passes all 94 focused routes in first/repeat/fresh sessions at 1,169/1,178 observations per session. The nine known immutable conflicts remain observed, two UPD-007 conflicts remain declared latent, and console/page/network/external-fixture errors are zero.
- Packed ESM/CJS proves the public exact-tuple extraction boundary. The 2+7 memory run covers 5,099 entities with retained heap median/p95 76,271/403,503 bytes; all nine extraction/destroy trials retain the authoritative canvas only until destroy and finish with zero DOM canvas, renderer, subscription, pending work, temporary image, or render texture ownership.
- Commit 58f2b66 adds the six-scale Chromium 4x extraction checkpoint and byte-identical durable package, memory, and performance artifacts. The full 2+7x10 raw samples retain unfavorable 5,000/production total p95 values of 64,231.0/110,215.2 ms; Core v1 is non-comparable, WebGPU was not run, and Windows native remains pending. Artifact digests, targeted six tests, scoped lint, typecheck, syntax, JSON, and whitespace checks pass.
- Paused on user request after planning the next shared-runtime tranche: ERR-001 plus CSM-005/006/007/008, covering 21 approved actions through the existing update/transaction adapter, expected-blind handler/fold/runtime, and focused Lab shell. ERR-003 is intentionally deferred to the asset/security tranche because it needs a controlled asset backend. No implementation or contract evidence changed, no gate was rerun, and the restart point is recorded in BRIEF.md.
- Closed `ERR-001` and `CSM-005/006/007/008` in product commit `7a5027a` and shared automation/Lab commit `2b59038`. Exact duplicate/reference validation, sync/async atomic refusal, redraw/merge persistence, live-overlay/frame ordering, transient presentation/export isolation, and real public failure probes raise coverage to 114 executable routes, 113 actual-producing routes, 59 stubs, 417 actions, and 229 action types. Full gates pass 157 files/1,389 tests, lint, typecheck, both builds, and canonical verification; the changed five headless routes pass first/repeat/fresh/destroy at 89/89 with zero browser errors, and packed ESM/CJS proves the exported validator and atomic `MISSING_TARGET` path. The full 99-route matrix, 2+7 memory, and performance matrix were intentionally not repeated because unchanged routes, renderer/resource/destroy ownership, and hot paths did not require them. No child agent or Vitest/Vite/Chromium process remains; app-owned MCP and node REPL services were not treated as test processes.

**2026-07-24**
- Closed the shared selection/host-interaction tranche `CSM-011/012/015/016/020/021`. Product commit `689de99` adds owner-qualified component selection plus immutable command target snapshots and pending/active/released progression without renderer identity or per-entity ownership; automation commit `c03c189` connects all 32 approved actions through one expected-blind pointer/selection runtime, fold, and six focused Lab routes.
- Coverage is 120 executable routes, 119 actual-producing routes, 53 stubs, 449 actions, and 247 action types. Full gates pass 157 files/1,402 tests, lint, typecheck, Core v2/Lab builds, and canonical 38-decision/32-negative/173-record verification. Headless Chromium first/repeat/fresh/destroy passes 111/111 assertions per session with deterministic observations, one transient canvas returning to zero, and no console/page/network/external-fixture errors.
- Packed ESM/CJS directly proves the new public revision, command APIs, component alias selection, aggregate WebGL consumption, and destroy cleanup. The full 105-route matrix, 2+7 memory, and performance matrix were not repeated because unchanged routes, weak-only command provenance, renderer/resource/destroy ownership, and measured hot paths did not require them. No child agent or owned Vitest/Vite/Chromium process remains; WebGPU, native Windows, and final independent release review remain pending.

**2026-07-27**
- Closed CSM-013/018/022/023/024 in product commit d5e81bd, shared expected-blind automation/Lab commit abaec4b, and package/lifecycle proof ec4e8b3. Coverage is 125 executable, 124 actual-producing, 48 stub routes, 476 actions, and 263 action types. Full gates pass 157 files/1,415 tests, lint, typecheck, both builds, and canonical 38-decision/32-negative/173-record verification. Five changed headless routes pass first/repeat/fresh/destroy at 96 matches plus five explicit immutable conflicts out of 101 assertions per session with zero browser errors; the full inventory is 110 routes, 1,479 assertions, 1,465 passes, fourteen observed conflicts, and two latent conflicts. Packed ESM/CJS proves editor mount and tooltip exports; 2+7 memory covers 5,099 entities at retained-heap median 88,063 bytes with terminal DOM/renderer/scheduler/subscription/canvas cleanup. Local tranche review corrected an unsupported selection-visual mode and the public viewport-policy probe field mapping. Performance was not repeated because aggregate render/upload and continuous pointer hot paths did not change; no child agent or owned Vitest/Vite/Chromium process remains, while WebGPU, native Windows, and final independent release review remain pending.
- Checkpointed the product half of `CSM-019/028/029/030/031` in `0564eae`. One expected-blind authoring planner now drives the existing staged semantic transaction, history, aggregate reconciliation, and selection authorities for seven create types, affine edits and idempotent alignment/distribution, advanced text paint, z-order/hierarchy, group/duplicate/paste/ungroup, stable relation rewriting, no-op detection, and atomic invalid branches. Four targeted files/52 tests, scoped lint, typecheck, and whitespace checks pass. The approved `CSM-030` host fixture names an `item` as a hierarchy parent even though PATCH MAP permits children only under `group`; the product truthfully returns `INVALID_MUTATION`, and the immutable expected remains unchanged for explicit comparison. Shared automation/Lab and tranche promotion gates are the restart point. No child agent or process owned by this worktree's tests remains.
- Closed CSM-019/028/029/030/031 in product 0564eae, shared expected-blind automation and focused Lab d532c36, and packed proof 3cd33d1. Coverage is 130 executable, 129 actual-producing, 43 stub routes, 495 actions, and 274 action types. Full gates pass 158 files/1,432 tests, lint, typecheck, both builds, and canonical 38-decision/32-negative/173-record verification. Five changed headless Chromium WebGL routes pass first/repeat/fresh/destroy at 103/109 matches plus six declared conflicts per session, deterministic cleanup, and zero console/page/network/external-fixture errors. Packed ESM/CJS directly exercises the authoring revision, planner, Engine.author(), and committed create result. The unchanged 110-route full matrix, 2+7 memory, and performance matrix were not repeated because no renderer/resource/destroy ownership or measured aggregate hot path changed. WebGPU, native Windows, and final independent release review remain pending.
- Closed the asset-ingestion tranche `ERR-003`, `AST-002`, `AST-003`, `SEC-001`, and `CSM-032` in product `ae5097b` and shared expected-blind automation/Lab `2eb941b`. Prefetch policy now rejects disallowed protocol/origin/credential/redirect/media/size/dimension/SVG inputs before attachment, target retry owns binding generations and stale-completion cleanup, and host clipboard/drop intake preserves CRLF text, stable IDs, input immutability, selection/history, and atomic multi-image rollback.
- Coverage is 135 executable routes, 134 actual-producing routes, 38 stubs, 514 actions, and 286 action types. The five headless WebGL routes pass first/repeat/fresh/destroy at 52 matches plus one declared conflict out of 53 assertions per session, with deterministic digests, canvas cleanup zero, and zero console/page/network/external-fixture errors. The immutable `AST-002` expected code remains `INVALID_ASSET_DESCRIPTOR`; actual product truthfully reports closed-registry `INVALID_VALUE`, raising the inventory to twenty-one observed and two latent conflicts.
- Targeted product tests pass 70/70. The full unit run recorded 1,437 passing tests and one stale route-count pin; after correcting only that static inventory assertion, its targeted file passes 11/11. Full lint, typecheck, Core v2/Lab builds, canonical verification, and packed ESM/CJS consumption pass. The 2+7 memory checkpoint covers 5,099 entities with retained-heap median 78,611 bytes and terminal DOM/scheduler/renderer/asset/pending-work release.
- The unchanged full browser and performance matrices were not repeated; the changed route subset and lifecycle ownership were exercised at the required risk boundary. Work is paused at this tranche checkpoint for an application restart. Headed final release verification, WebGPU, native Windows, and final independent release review remain pending.
- Closed the editor-workflow tranche `CSM-025/026/027/033/034` in product `105eb7b` and shared expected-blind automation/Lab `330dacf`. One logical authority implements grid, relation, text, host-confirmed cascade delete, and the twelve-kind mutation/history matrix through existing Engine transaction, selection, history, and aggregate publication paths without entity listeners or expected-data imports. Coverage is 140 executable, 139 actual-producing, 33 stub routes, 545 actions, and 304 action types. Full verification passes 160 files/1,454 unit tests, lint, typecheck, both builds, canonical verification, packed ESM/CJS, and 2+7 lifecycle memory; the five changed headless WebGL routes pass first/repeat/fresh/destroy at 110/110 assertions with deterministic cleanup and zero browser errors. Memory covers 5,099 entities at retained-heap median/p95 87,323/402,107 bytes with no lifecycle or DOM failures. The unchanged full 125-route browser and performance matrices were not repeated; no expected evidence changed, and work is paused at this clean tranche checkpoint for an application restart.
- Closed `DET-001/002/003`, `ANI-003`, and `LIF-006` in product `bfd3b92` and shared expected-blind automation/Lab/evidence `6953b08`. Coverage is 145 executable, 144 actual-producing, and 28 stub routes with 573 actions and 317 action types. An engine-local flat lifecycle authority authenticates cancellable asset/extraction tokens, settles presentation at committed destinations, atomically rejects failed surface suspension, cancels viewport/pointer/transformer ownership, and counts exactly one resume publication; ANI-003 now sends the approved action ID through the real transaction/history path.
- The five changed headless WebGL routes pass first/repeat/fresh/destroy at 49/49 assertions per session with deterministic actuals, canvas `0 -> 1 -> 0`, real WebGL2 draws, and zero console/page/network/external-fixture errors. The 130-route registry totals 1,800 assertions with 1,779 matches, twenty-one observed immutable conflicts, and two latent conflicts. Packed ESM/CJS directly exercises the new revision, authority, visibility methods, cancellation, and one-frame resume boundary.
- Full lint, typecheck, Core v2/Lab builds, and canonical 38-decision/32-negative/173-record verification pass. The first full unit attempt produced four CPU-contention timeouts that all passed alone; the serial rerun reached 1,471/1,472 and exposed only a stale static memory-script pin, which was corrected and passed 3/3 targeted. Per risk cadence, assertion-only correction did not trigger another unrelated full/package/performance matrix.
- The final 2+7 memory run covers 5,099 entities with retained-heap median/p95/max 76,659/399,531/399,531 bytes, zero lifecycle failures, and terminal release of page tokens, gestures, pointer capture, DOM, canvas, scheduler, renderer, subscriptions, and pending work. Performance and the unchanged full 130-route browser matrix were not repeated because aggregate renderer hot paths did not change. No child agent is active; owned browser/Vite/Vitest processes are closed. Headed release proof, WebGPU, native Windows, and final independent release review remain pending.
- Closed `CSM-014` in `154c4c8` through the existing update/transaction handler, fold, runtime, and focused Lab shell. Three declared dashboard columns atomically update bar geometry, label text, tint, and visibility; a fresh Engine restores the persisted percent column, and the isolated invalid-column branch retains the prior complete scene. Coverage is 146 executable, 145 actual-producing, and 27 stub routes with 578 actions and 319 action types.
- Targeted handler/fold/runtime tests, full lint and typecheck, Core v2 Lab build, and canonical 38-decision/32-negative/173-record verification pass. The serial Core v2 unit run passed 1,246 tests and exposed only a stale runtime-map pin; its corrected focused regression passes. Headless Chromium 143 first/repeat/fresh/destroy passes CSM-014 at 21/21 assertions per session with deterministic digest, canvas cleanup zero, and zero console/page/network/external-fixture errors.
- The browser registry is now 131 routes and 1,821 assertions: 1,800 matches, twenty-one observed immutable conflicts, and two latent conflicts. Package, 2+7 memory, full browser, and performance matrices were not repeated because package exports, renderer/resource/destroy ownership, and hot paths did not change. No child agent or owned Vitest/Vite/Chromium process remains; work is paused for restart before the Pixi/package tranche.
- Checkpointed the PIX-001/002/003/005 public product substrate: explicit WebGL2 initialization and pre-allocation WebGL1 rejection, official opt-in PixiJS DevTools Application registration, detached Application/stage/aggregate render-owner probes, and renderer-loss/listener cleanup facts that remain safe after destroy. Four targeted tests, scoped lint, and typecheck pass. The unconnected automation draft was not retained; shared expected-blind handlers/folds, focused Lab routes, changed-route headless WebGL, and the renderer-ownership 2+7 memory checkpoint are the restart point. Coverage remains 146/145/27, no child agent is active, and no browser gate was started before the requested application restart.
- Closed PIX-001/002/003/005 shared expected-blind automation and focused Lab in e9a3b95 on top of the checkpointed product probes. Coverage is 150 executable, 149 actual-producing, 23 stubs, 586 actions, and 325 action types. Headless Chromium first/repeat/fresh/destroy passes 34/34 PIX assertions per session with deterministic actuals, canvas 0 -> 1 -> 0, real WebGL2 draws for PIX-001/002/003, zero browser errors, and eight native Windows cells kept pending; PIX-005 classifies all eight renderer-loss boundaries after adding pre-render/pre-extraction RENDERER_LOST guards. Full Core v2 unit passes 1,263 tests with lint, typecheck, Lab build, canonical verification, and 2+7 memory over 5,099 entities at retained-heap median 88,275 bytes. Local review fixed legacy injected-renderer terminal-probe cleanup and tightened fixture-digest binding. Package/performance/headed gates were not run because those boundaries did not change; no child agent or owned Vitest/Vite/Chromium process remains, while WebGPU, native Windows, headed release proof, package/performance promotion, and final independent review remain pending.
- Checkpointed the packed Core v2 consumer boundary in 53740ff and d712cac. The published package now includes a thin Engine-delegating host adapter, four strict TypeScript examples, and public integration documentation; final committed-HEAD verification passes offline install, strict declarations, ESM/CJS, production bundling, 38/38 expected-blind CSM executions, builtin-asset multi-instance isolation, extraction, and destroy cleanup. The 109-file artifact SHA-256 is 9d186b59270753a437d11e400b942513312831a04f3cd4475e21651f1c0e16d1 with zero source maps or restricted evidence, zero comparison cleanup failures, and zero console/page/network errors. Targeted fold/browser tests, scoped lint, typecheck, and canonical 38-decision/32-negative/173-record verification pass. Coverage remains 150/149/23 until PKG-001..005 are connected after restart; headed, memory, and performance gates were not repeated because this checkpoint did not change renderer ownership, destroy paths, or measured hot paths. No child agent or owned browser/Vite/Vitest process remains.
- Closed PKG-001..005 in implementation commit e127627 and verification commit 8a0d390 through one browser-safe expected-blind packed-evidence runtime, shared handlers/fold, and five focused Lab routes; PKG-003 additionally proves live two-Engine isolation and recreate cleanup. Coverage is 155 executable, 154 actual-producing, 18 stub routes, 600 actions, and 338 action types. Targeted headless Chromium first/repeat/fresh/destroy passes every PKG assertion with zero browser errors and canvas cleanup; the registry is 140 routes and 1,891 assertions with 1,870 matches, twenty-one observed immutable conflicts, and two latent conflicts. Full gates pass 165 files/1,505 unit tests, lint, typecheck, Lab build, and canonical 38-decision/32-negative/173-record verification. Packed, memory, performance, and headed gates were not repeated because package exports, renderer ownership, destroy paths, and measured hot paths did not change. No child agent or worktree-owned browser/Vite/Vitest process remains; headed release proof, WebGPU, native Windows, performance promotion, and final independent review remain pending.
- Closed PRF-001..006/009 through the shared expected-blind performance workload/runtime/handler/fold and seven focused Lab routes in implementation and evidence commits faac03b, ef1dc01, 9918556, 4e4fdb8, d862fff, and e079202; follow-up 00a4284 preserves a monotonic two-session publication timeline and 1c16fff registers honest browser comparison. Coverage is 162 executable, 161 actual-producing, 11 stubs, 614 actions, and 351 action types. The preserved Chromium 4x two-warmup/seven-sample six-scale raw digest is 2a8a8443c2e7372192c589fac0552b2a1ca5052ffa726af5d28681692b72e535 with zero browser/lifecycle errors. Headless first/repeat/fresh/destroy proves deterministic actuals, real WebGL2 draws, and canvas cleanup for all seven routes: PRF-009 passes 9/9 while PRF-001..006 expose fourteen measured latency/long-task deficits separately from twenty-one immutable conflicts. The registry is 147 routes and 1,946 assertions with 1,911 matches, twenty-one observed immutable conflicts, fourteen performance deficits, and two latent conflicts. Full gates pass 167 files/1,515 tests, lint, typecheck, both builds, and canonical 38-decision/32-negative/173-record verification. Package, 2+7 memory, and headed release were not repeated because exports, renderer ownership, and destroy paths did not change; native Windows and WebGPU remain pending. Local review found and fixed the PRF-009 backward-clock, missing volatile inventory, completion-timeout, and sequential-canvas assumptions. No child agent or worktree-owned browser/Vite/Vitest process remains.
- Accelerated flat scene mutations in `464bfee` with materializer-branded structural sharing, authenticated selected-root parsing, parser-option guards, parser-owned dense planning, dirty renderer ranges, incremental Engine indexes, and owned frozen history snapshots. Caller-owned or incomplete-hint input, changed parser options, hierarchy, relations, diagnostics, identity changes, and large writes fail closed to the canonical full path. Full gates pass 168 files/1,522 tests, lint, typecheck, both builds, and canonical verification.
- The committed six-scale Chromium 4x matrix in `97d7059` preserves 2+7 raw digest `5181d36ca690175bd67f463e0eac6cdc969fe0102c3fdd8c68a2d0f70af25a15`. Median 2,000-record bar/text/10% bulk improves 45–66%; 5,000-record move/rotate/10% bulk improves 64–75% and whole-scene bulk 21%. Edge auto-pan regresses 23%, small-path noise remains visible, and the same fourteen approved-budget deficits remain observed rather than waived.
- All seven PRF focused routes pass deterministic headless first/repeat/fresh/destroy with real WebGL2, error counts zero, and terminal canvas cleanup; PRF-009 remains 9/9. Memory evidence `69cd259` passes 2+7 over 5,099 entities at retained-heap median/p95 89,019/406,315 bytes with DOM/scheduler/renderer/history release. No package gate was repeated because exports did not change, and no worktree-owned browser/Vite/Node test process remains.
- Closed SEC-002..004 and OPS-001..002 through the shared operations/extraction product authority, expected-blind runtime, handler/fold, and focused Lab routes in da5c38c, 962ff1e, d3d91f9, 98442e5, 9c09ad6, d0f50cd, bb8fe89, and 5454940. Coverage is 167 executable, 166 actual-producing, 6 stubs, 626 actions, and 362 action types. Targeted headless first/repeat/fresh/destroy matches all 33 new assertions with real WebGL2 evidence where applicable, deterministic actuals, terminal canvas cleanup, and zero browser errors; full Core v2 gates pass 139 files/1,316 tests, lint, typecheck, both builds, and canonical 38-decision/173-record verification. Reproducible packed ESM/CJS covers all 38 journeys with audit 0, approved license inventory, digest-bound SPDX-lite SBOM, and canvas zero; final 2+7 memory covers 5,099 entities at median/p95 86,999/406,663 bytes. Performance and headed browser were not repeated because the aggregate hot path did not change and headless satisfies this tranche; all worktree-owned test/browser processes are closed, while final independent review, WebGPU, native Windows, the full 152-route checkpoint, ACC-001..003, and MIG-001..003 remain pending.
- Closed ACC-001..003 in product commit 47ba5cf and shared expected-blind automation/Lab evidence commit 010b5b6. Coverage is 170 executable routes, 169 actual-producing routes, 3 MIG stubs, 636 actions, and 371 action types. Full gates pass 140 Core v2 files/1,324 tests, lint, typecheck, both builds, and canonical 38-decision/32-negative/173-record verification. Headless first/repeat/fresh/destroy matches all 20 accessibility assertions with real WebGL2, three Pixi shadow nodes, trusted accessibility click, one delegated root listener, zero entity listeners, deterministic actuals, terminal canvas zero, and no browser errors. Packed ESM/CJS/types plus all 38 journeys pass. The overlay-aware 2+7 lifecycle run covers 5,099 entities at 89,095-byte median retained heap and releases logical nodes, surface, host DOM, listener, renderer, scheduler, and canvas ownership. Performance was not repeated because aggregate render/upload hot paths did not change; headed release, WebGPU, native Windows, the full 155-route rerun, MIG-001..003, and final independent review remain pending.
- Closed MIG-001/002/003 in product 91694cd, lifecycle fix a812225, shared expected-blind automation/Lab 2b59c5c, public migration docs cd32eca, and refreshed packed evidence f321fe8. Coverage is 173 executable routes, 172 terminal-actual routes, zero stubs, 646 actions, and 381 action types; DAT-008 remains the intentional malformed-action stop. Full gates pass 142 files/1,345 tests, lint, typecheck, both builds, canonical 38-decision/32-negative/173-record verification, and local independent review. Full headless Chromium first/repeat/fresh covers 158 routes and 2,028 assertions at 1,993 matches, twenty-one declared immutable conflicts, fourteen measured deficits, and two latent conflicts with zero console/page/network errors; it found and closed the inactive AccessibilitySystem DOM dereference and one package-observable drift. Packed ESM/CJS/types plus all 38 journeys and four examples pass against cc691f4c855eed102d672c51e7284ed5bba0f1ebac1c9e8ae8794c33b1140eef with audit/package hygiene and canvas cleanup zero. Final 2+7 memory covers 5,099 entities at 88,231-byte median retained heap and releases renderer, scheduler, DOM, listeners, and canvas. The unchanged hot path retained its 18-run/162-trial Chromium 4x evidence: material large-scene wins remain, edge auto-pan regresses 23%, and fourteen budgets remain deficient. Headed NVDA, native Windows Chrome/Edge/performance, and WebGPU stay pending; no child agent or worktree-owned test/browser process remains.

**2026-07-28**
- Added the persistent human-operated Core v2 workbench in `1614b05` and its independent headless route gate in `d285f17`. All 173 cases and 646 approved actions map to shared direct controls without importing normalized expected evidence; the transient exact runner remains available below the manual canvas.
- Representative WebGL2 interaction passes direct click selection, box selection, move/resize preview and one-step commit, Escape cancellation, undo button, redo shortcut, paused/resumed bar animation, PNG extraction with authoritative-canvas retention, and destroy/re-init. The all-route checkpoint passes 189/189 checks across 173/173 routes with one live canvas per route and zero console/page/network errors.
- Tranche gates pass 143 Core v2 files/1,350 tests, full lint/typecheck, Core v2 Lab production build, and canonical 38-decision/32-negative/173-record verification. Package, 2+7 memory, and performance matrices were not repeated because package exports, renderer/resource ownership, destroy implementation, and measured hot paths did not change. Test-owned Chromium and temporary Vite processes close in `finally`; the port-4176 user dogfood server is intentionally left running.

**2026-07-28**
- Localized the complete human-operated and exact-runner Lab presentation into Korean without changing case IDs, action traces, product execution, or immutable expected evidence. All 173 case titles have an exact Korean catalog entry; mode bars, tool drawers, button effects, statuses, errors, inspector labels, and route controls are localized while raw method names, JSON fields, and internal handler IDs remain explicitly marked technical identifiers.
- Added an always-visible three-step walkthrough, an expanded screen map, per-tool descriptions, per-button effect text and tooltips, Korean accessibility labels, and a Korean-first font stack. A 1,720 px visual inspection confirms the guide, canvas, right-hand controls, result strip, and exact runner remain readable without layout breakage.
- Targeted verification passes 3 files/44 tests, scoped lint, typecheck, the Core v2 Lab production build, and canonical 38-decision/32-negative/173-record verification. Headless Chromium passes 189/189 checks across all 173 localized routes with zero console/page/network errors; every owned test browser and temporary server closes, while the user dogfood server on port 4176 intentionally remains running. Package, memory, and performance gates were not repeated because no package, renderer/resource ownership, destroy, or measured hot path changed.

**2026-07-28**
- Optimized the 5,000-bar whole-animation/pan path in `3ca9873`. The Lab now submits one `Float64Array` height batch; Engine preserves strict validation, duplicate/missing-target atomic refusal, one history command, stable IDs/component identity, and direct canonical projection. The hot path uses streamed exact FNV-1a64 hashing, structurally shared roots, reusable presentation tuples, visible-only Mesh uploads with deferred offscreen catch-up, large fallback-text chunk culling, O(1) animation/status probes, and one host-owned viewport-first frame loop.
- The same-clock frame scheduler publishes three cheap viewport frames between heavy whole-animation steps while a large scene is actively panned. A direct 4x comparison increased actual canvas publications from 27 to 41 over the same 12-step drag without materially changing drag wall time. A per-bar BitmapText spike was rejected because it worsened the 4x pan result; the guarded Text lane remains chunked instead.
- Evidence commit `416727c` adds a headless WebGL2 2+7 checkpoint with raw action, pan, rAF, actual canvas-publication, long-task, interpolation, viewport, error, and cleanup samples. Final 1x median/p95 is 220.7/275.2 ms for the action and 1,605.3/1,696.0 ms for pan, with 31.8 canvas fps and 50.8 ms median run-level gap p95. Chromium 4x is 864.7/954.3 ms, 2,311.6/2,653.7 ms, 17.3 fps, and 96.2 ms. All 18 sessions visibly interpolate, move the viewport, destroy to zero canvas, and have zero console/page/network/external errors. The comparable exploratory pre-optimization medians were 1,498.5 ms action and 5,611.1 ms pan, but their pre-harness raw samples are unavailable and are labeled noncanonical.
- Final tranche gates pass 175 files/1,591 unit tests, full lint/typecheck, Core v2 and Lab builds, canonical 38-decision/32-negative/173-record verification, representative real-WebGL2 manual interaction, packed ESM/CJS/types plus 38 journeys, and a 2+7 lifecycle proof over 5,099 entities. The observed current memory median was 78,723 bytes with DOM/scheduler/renderer release; frozen result files were restored after verification. Every temporary headless browser and test server closes; the user dogfood server is the only intentional survivor. WebGPU was not run and native Windows remains pending.

**2026-07-28**
- Completed the 5,000-entity interaction tranche across pointer click/shift/empty, box/paint selection, hover/context menu, pan/zoom/reset/fit, transformer move/resize/rotate/cancel, keyboard nudge, undo/redo, group/front/align/distribute/duplicate/create/delete, all/partial/selected bar animation, random/selected text, assets, accessibility, resize, capture, page lifecycle, and destroy/re-init. Product hot paths now use a screen-region index, direct stable source slots, bounded structural/parser/diagnostic caches, incremental bar/text/structure projection, exact range publication, owned immutable history roots, and no per-entity listener/ticker/closure.
- The final headless WebGL2 2+7 evidence at `evidence/interaction-performance-5000.json` passes both 1x and Chromium 4x with every raw sample, median/p95/min/max, zero threshold violations, and zero console/page/network/external errors. At 1x, click/shift/box/paint p95 is 0.4/0.3/0.4/0.5 ms, pan/zoom frame p95 is 6.6/4.5 ms, all-bar-animation pan frame p95 is 12.3 ms, transformer move preview/commit is 5.9/45.2 ms, undo/redo product p95 is 16.6/12.2 ms, whole-bar scheduling is 108.8 ms, and random text is 106.4 ms. At 4x, the corresponding continuous-frame values are 26.4 ms pan, 20.1 ms zoom, and 42.8 ms animation-pan; this proxy tail remains visible and native Windows is pending.
- Relative to the earlier same-task exploratory checkpoint whose raw file was superseded, pointer selection falls from 5.5--6.7 ms to 0.3--0.5 ms p95, delete from 427.1 to 120.8 ms, duplicate from 140.0 to 95.6 ms, group from 104.0 to 68.0 ms, whole-bar scheduling from 135.5 to 108.8 ms, random text from 123.4 to 106.4 ms, and undo/redo from about 87/29 to 16.6/12.2 ms. These comparisons are labeled exploratory because the predecessor raw JSON was overwritten; the final 2+7 evidence is canonical.
- A browser gate exposed two independent lifecycle edges and both were closed without changing expected evidence: Pixi Prepare now excludes mutable Canvas Text while still pre-uploading aggregate geometry and image leaves, preventing stale prepared text batches after a later text update; host alias asset load/unload now targets the canonical `alias:*` scene binding. Packed CSM-018 also proved that an empty selection before first dataset load must return empty geometry without invoking the parser.
- Final tranche verification passes the 176-file/1,610-test full unit checkpoint plus post-fix 35- and 32-test regressions, full lint/typecheck, Core v2 and Lab builds, canonical contract verification, 31/31 product browser checks, and 189/189 human-Lab checks across 173 routes. Packed ESM/CJS/types, four examples, and 38/38 journeys pass against artifact `c762337093139d7b42a089a5f8cdc83f0b6705a4e70d090f2755cb0c2253c966`; the 2+7 memory run covers 5,099 entities at 76,991-byte median retained heap with terminal DOM/canvas/scheduler/renderer release. Capture (1x p95 1,018.8 ms), accessibility tree creation (199.2 ms), and re-init (892.1 ms) remain honestly classified as one-shot costs. All temporary browsers close; the port-4176 dogfood server intentionally remains available. WebGPU and native Windows remain pending.

**2026-07-29**
- Promoted the 5,000-entity interaction optimization checkpoint. Retained Mesh records now use Pixi's default public batch mode, detach coarse offscreen chunks, and switch between precise idle record culling and pre-sorted coarse animation chunks. Exact parser-owned reconcile visits only dirty identities, the renderer owns an O(changed) slot index, geometry-only transformer transactions reuse component/text semantics, and bounded stable-record/parser/text paths avoid repeated full-scene work. A regression test caught and fixed the precise-to-coarse transition when chunk visibility itself did not change.
- The full headless WebGL2 2+7 interaction matrix covers pointer, selection, transformer, viewport, keyboard, history, animation, text, authoring, assets, accessibility, extraction, resize, and destroy/re-init. At 1x, pointer/box/paint p95 is 0.1--0.8 ms, pan/zoom is 1.6/1.5 ms, animation-pan is 8.4 ms, move commit is 8.3 ms, and nudge is 9.2 ms. Chromium 4x records 1.1--3.3, 7.6/3.9, 31.3, 35.5, and 36.2 ms respectively. Against the prior canonical checkpoint, pan/zoom falls 66.7--80.6% and animation-pan falls 26.9--31.7%; the 4x animation frame remains above 16.7 ms.
- The dedicated 5,000-bar animation-plus-pan checkpoint improves 1x action/pan p95 from 275.2/1,696.0 to 133.5/664.4 ms and canvas publication from 31.8 to 61.6 fps. Chromium 4x improves 954.3/2,653.7 to 510.9/953.0 ms and 17.3 to 50.4 fps. Every raw sample, warmup, median/p95/min/max, visible interpolation, viewport movement, and zero-canvas cleanup fact is retained. The 4x proxy still records two long tasks per pan run with a 107 ms maximum; native Windows remains pending.
- Tranche gates pass the logical 146-file/1,403-test Core v2 checkpoint, full lint/typecheck, Core v2/Lab builds, canonical 38-decision/32-negative/173-record verification, both performance report verifiers, 31/31 product browser checks, and 189/189 human-Lab checks over 173 routes with zero console/page/network errors. The renderer-ownership 2+7 memory gate releases every renderer/scheduler/DOM/listener/canvas owner at an 86,403-byte median, 12.2% above the prior checkpoint but within budget. Packed consumer was not rerun because exports, dependencies, assets, and consumer boundaries did not change; artifact `c762337093139d7b42a089a5f8cdc83f0b6705a4e70d090f2755cb0c2253c966` remains the current proof. Temporary headless browsers and Vite servers close in `finally`; the user dogfood server on port 4176 intentionally remains. Headed assistive-technology, WebGPU, and native Windows Chrome/Edge performance remain pending.
- Added release-qualification runners in `e3ea1b5` and digest-bound evidence in `5b8f0fa`. The default remains headless Playwright Chromium; the same product and 173-route Lab checks can now target headed `chrome`, `msedge`, or an exact executable path. `--native-windows` requires an actual Windows host, headed pinned browser, and stable matrix cell ID; the performance runner uses unthrottled native timing and a cell-owned output directory instead of overwriting the Chromium 4x proxy.
- The experimental WebGPU run activates the real PixiJS `webgpu` backend on Chromium 143/Apple Metal 3 with no silent WebGL fallback. It passes 18/18 checks over 5,000 source records and 25,499 render entities, including full-bar intermediate/final frames, text, root pointer selection, zoom/pan, assets, extraction, resize, destroy/re-init, final zero canvas, and zero console/page/network errors. Product WebGL checks pass 31/31 and the SHA-bound all-route Lab checkpoint passes 189/189 over 173 routes.
- A strict release-readiness verifier reads the approved OQ-024/025/029 fixtures directly, hashes eight local candidate artifacts, rejects prohibited paths or digest drift, and keeps `releaseVerified: false` while native evidence is absent. Current local status is `pass`; external pending items are the eight headed Windows 10/11 Chrome/Edge latest-two cells, NVDA and real input-device traces, the exact N100 2+7 performance/ten-cycle lifecycle profile, packed non-mock production-host 38 journeys, security, canary/rollback, and independent review.
- Checkpoint gates pass 147 Core v2 files/1,408 tests, full lint/typecheck, canonical 38-decision/32-negative/173-record verification, release-readiness local verification, WebGPU 18/18, product browser 31/31, human-Lab 189/189, and a contract-performance smoke with zero lifecycle/browser failures. Package, 2+7 memory, and full WebGL performance matrices were not repeated because exports, dependencies, renderer/resource ownership, destroy paths, and production hot paths did not change. Every test-owned headless browser and temporary Vite server closed; the user dogfood server on port 4176 intentionally remains.
- Closed the native release ownership tranche in e1e2b77 and evidence commit fb89727. The shared manifest contract/generator reserves five unique digest roles for each of eight Windows 10/11 Chrome/Edge latest-two cells plus actual-host/security/migration/review; a satisfiable positive proof and 15 drift probes reject headless or WebGPU substitution, placeholders, cell/package drift, null or over-budget timing, nine lifecycle cycles, NVDA gaps, digest/role substitution, mock host evidence, and pending review. The memory runner now shares exact browser launch guards, refuses non-Windows or software-GPU native claims, measures ten native ownership cycles around a forced-GC window, and proves terminal canvas/listener/observer/ticker/texture/callback release. Final local 2+7 evidence over 5,099 entities reports 82,399-byte retained-heap median, 1.72 MiB forced-GC growth, owner delta zero, and zero browser errors; it remains non-native because the macOS headless run uses SwiftShader. Gates pass 148 files/1,409 tests, full lint/typecheck, canonical 38-decision/32-negative/173-record verification, and local release status pass. Product builds, browser/package, and performance matrices were not repeated because renderer, Lab, exports, dependencies, and hot paths did not change. Exact headed Windows/N100/NVDA/input-device, real production-host, security, migration/rollback, and independent-review evidence remains pending; all test-owned processes closed and the port-4176 dogfood server intentionally remains.
- Applied the user-approved completion boundary without changing immutable expected evidence or weakening strict release verification. The local production candidate remains proven by 173/173 executable Lab mappings, 189/189 manual checks, 31/31 product browser checks, 38/38 packed journeys, 148 files/1,409 tests, lifecycle owner delta zero, and passing 5,000-entity interaction/bar evidence. Exact Windows/N100/NVDA/real-input, production-host, migration rehearsal, security, and independent-review artifacts remain explicitly pending; release-readiness therefore truthfully stays pending-external-evidence with local pass, native pending, and releaseVerified false. Only durable context changed, so no product, browser, package, memory, or performance gate was rerun.
- Readable content-orientation tranche: aggregate mesh, particle, text, semantic geometry를 동일한 half-plane 180도 규칙으로 통합하고 owner-fit cache를 제거했다. 정확한 0/89/90/131/180/229/269/270/315도, flip X/Y/XY, 부분 bar, containment를 검증했다. Core v2 148 files/1,427 tests, typecheck, lint, Lab build, canonical 173 verifier, representative manual Lab, LAY-004/REN-006/REN-011 first-repeat-fresh headless가 통과했다. 5,000 interaction smoke는 pan frame p95 2.3 ms, animation-pan 9.0 ms, world rotate 29.5 ms, flip X/Y 18.8/18.4 ms였고 browser errors는 0이었다. Immutable expected는 유지하고 LAY-004 2개와 REN-011 3개 conflict actual을 pin했다.
- Closed the local Core v2 production candidate around product/evidence SHA 2b995ab054d441f48c964b995713fb2388dea04b. Readable radial orientation follows item/world rotation and adds only the upside-down half-turn; 43 targeted geometry/render tests and LAY-004 headless first/repeat/fresh pass with deterministic cleanup, while its two frozen screen-lock conflicts remain declared. Full verification passes 148 files/1,430 tests, lint, typecheck, both builds, canonical 38-decision/32-negative/173-record checks, all 158 browser routes at 1,988/2,028 matches plus 26 immutable conflicts and 14 measured deficits, 31 product checks, 189 manual-Lab checks, 38 packed journeys, 2+7 memory, 18 renderer runs/162 trials, WebGPU 18/18 experimental checks, release local binding, and fifteen native drift probes. Evidence is refreshed in 242fb0e; test-only proof binding and parser timeout are fa8dee1 and 7360ccb. All test-owned browsers and temporary servers are closed, port 4176 remains intentional, and strict release stays pending-external-evidence for Windows/N100/NVDA/input, actual host, security, migration/rollback, and independent review.

**2026-07-29**
- Added the exploratory 10,000-record Lab extension in `8db2f0c` without changing the approved exact route or performance matrix. The performance Playground accepts `dataset=10000`; every human-operated route can switch from its canonical input to a deterministic 10,000-record scene through the Korean data panel while preserving `node-0..node-9999`, component identity, frozen input, and explicit exploratory labeling.
- The dedicated headless Chromium 143 Mesh WebGL smoke passes eight checks: the Playground loads 50,999 renderer entities, the manual Lab loads 10,005 roots and 30,000 components, the visible all-bar action changes heights, long animation remains active during viewport movement, and both active sessions destroy to zero canvas/bridge with zero console/page/network errors. Native Windows remains pending.
- Tranche gates pass 148 Core v2 files/1,432 tests, full lint/typecheck, the Core v2 Lab production build, canonical 38-decision/32-negative/173-record verification, and the repeatable `verify:lab:core-v2:10000` smoke. An initial full-unit run shared resources with lint/typecheck and timed out two existing long tests; both passed alone and the isolated full rerun passed 1,432/1,432. Package, 2+7 memory, WebGPU, and full performance matrices were not repeated because exports, dependencies, renderer/resource ownership, destroy code, and production hot paths did not change. All test-owned browser/server processes closed; only the intentional port-4176 dogfood server remains.

**2026-07-29**
- Lowered only the human-operated Lab viewport floors in `2f18468` from 10%/50% to 2.5%, preserving the Core v2 product defaults, exact contract runner, input, identity, and renderer hot paths. The configured floor now applies consistently to fit, programmatic zoom, root wheel zoom, and the visible Korean 축소 control.
- Headless Chromium 143 Mesh WebGL proves the 10,000-record Playground fits at 6.03%, the 10,000-record manual Lab fits at 4.33%, and both can continue to 2.5%. Cursor-centered wheel drift is 0.22 CSS px, long animation still overlaps viewport pan, both sessions destroy to zero canvas/bridge, and console/page/network errors remain zero.
- Targeted 31/31 tests, full lint, typecheck, the Core v2 Lab build, and canonical 38-decision/32-negative/173-record verification pass. Package, 2+7 memory, WebGPU, and performance matrices were not repeated because exports, dependency/asset packaging, renderer/resource ownership, destroy paths, and production hot paths did not change. The headless browser and temporary Vite server closed in `finally`; port 4176 remains the intentional user dogfood server and native Windows remains pending.

**2026-07-29**
- Exposed the exploratory 10,000-record choice in the always-visible Lab route controls in `edd726c`. The new `직접 조작 크기` selector loads the persistent manual Pixi scene immediately and stays synchronized with the detailed data panel; the adjacent `정확 실행 크기` selector remains limited to the approved canonical corpus.
- Targeted 43/43 tests, scoped lint, typecheck, Lab build, canonical 38-decision/32-negative/173-record verification, and the headless 10,000-record WebGL smoke pass. The smoke selects 10,000 from the new top control, loads 10,005 roots/30,000 components, overlaps animation with pan, destroys to zero canvas, and reports zero console/page/network errors.
- The stale port-4176 Vite/HMR process that had produced Pixi's duplicate `shape-builder` registration was replaced with a clean server. A separate headless check against the restarted user URL sees the visible `10,000개 객체 · 탐색용` option, loads it successfully, and records no browser errors. Package, memory, WebGPU, and performance gates were not repeated because their owned paths did not change; native Windows remains pending.

**2026-07-29**
- Corrected readable bottom-bar placement in `c6bf554`. Upright bars now transform their owner-relative center with the same reflection/half-turn used by their aggregate basis, while text retains its authored center. Vertical flip and 180-degree correction keep bars inside the frame and on the visible bottom; height animation changes the top edge while the bottom edge stays fixed.
- Core v2 passes 148 files/1,440 tests, typecheck, full lint, Core v2 and Lab builds, and canonical 38-decision/32-negative/173-record verification. Targeted LAY-004 first/repeat/fresh remains deterministic at 9/11 with exactly the two predeclared immutable conflicts and zero browser errors.
- The new headless WebGL orientation mode directly checks vertical flip, in-flight 2,000 ms bar animation, 180-degree correction, canvas ownership, and console/page/network errors. An initial representative manual run timed out at its earlier direct-click precondition before reaching orientation; the isolated changed-path run passes and closes its browser/server in `finally`.
- Because aggregate animation geometry changed, the 5,000-bar pan checkpoint was repeated to an external temporary output with 2 warmups and 7 measured samples for both profiles. Chromium headless 1x reports action median/p95 117.7/126.0 ms, pan 624.0/728.0 ms, 61.0 median canvas FPS, and 18.5 ms frame-gap p95; 4x reports 457.9/554.7 ms, pan 870.3/959.1 ms, 50.6 FPS, and 59.3 ms frame-gap p95. Both pass. Package and 2+7 memory were not repeated because exports, dependency packaging, ownership, and destroy paths did not change. Native Windows remains pending.
