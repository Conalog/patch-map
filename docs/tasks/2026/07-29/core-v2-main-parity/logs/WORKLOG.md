# Worklog

**2026-07-29**

- Created the narrow read-only `main` runtime-oracle policy and restartable parity task after explicit user approval. The comparison will use identical public inputs and independent browser observations without inspecting or copying `main` implementation source.
