**2026-03-25**
- 배경: 사용자는 `docs/reference/patchmap`를 중심 증거로 삼되, 언어와 프레임워크에 종속되지 않는 단일 `spec.md`를 요구했다.
- 선택지: 기존 reference 문서를 그대로 확장할지, Spec Kit 구조에 맞는 신규 spec을 만들지, 둘 다 병행할지 검토했다.
- 결정: reference 문서는 근거 자료로 유지하고, Spec Kit 표준 위치인 `specs/001-patchmap-spec/spec.md`에 단일 호환성 명세를 작성한다.
- 영향: 명세는 구현 설명보다 관찰 가능한 계약을 우선시하며, 세부 규칙은 소스와 테스트로 재검증해야 한다.

**2026-03-25**
- 배경: spec 이후에 plan/tasks 등 구현 전제 문서를 생성했지만, 사용자는 언어와 라이브러리가 정해지지 않은 상태에서는 그런 산출물이 부적절하다고 지적했다.
- 선택지: 생성한 구현 문서를 유지할지, 참고용으로만 남길지, 전부 제거하고 spec 중심 상태로 되돌릴지 검토했다.
- 결정: 구현 전제 산출물(`plan.md`, `tasks.md`, 보조 계약 문서, agent-context 생성물)은 제거하고, spec과 관련 task-context만 유지한다.
- 영향: 저장소 상태가 다시 “언어 독립적 명세와 feasibility 판단” 범위로 정리되며, 이후 concrete stack이 정해진 뒤에만 새 plan/tasks를 만드는 것이 맞다.
